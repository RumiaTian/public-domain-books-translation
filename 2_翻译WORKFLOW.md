# 阶段二：翻译执行工作流（2_翻译WORKFLOW.md）

本文件是**翻译执行阶段的标准操作手册**。
面向 `翻译项目/` 目录下的 647 本精选公版书目，按照任务队列推进高质量双语对照翻译。

---

## 一、项目架构与规范

每个翻译项目位于 `翻译项目/<项目名>/` 下，标准结构如下：

```text
翻译项目/<项目名>/
  ├── 项目说明.md           # 领域、形态、输出粒度、篇目清单
  ├── 术语表.md             # 翻译硬约束层（人物/地名/专名/术语的统一译法）
  ├── Plan.md              # 单篇执行 9 步指南与运行日志
  ├── translation_queue.csv # file, size_kb, status (todo / doing / done)
  ├── 原文/*.md            # 转换自 EPUB spine 的分章源文（UTF-8 无 BOM）
  └── 译文/                # 双语对照产出目录 (*.zh-CN.md)
```

---

## 二、两大执行模式（按内容形态分派）

由 `项目说明.md` 中的内容形态决定：

| 内容形态 | 判据与文体 | 执行模式 | 运行机制 |
| :--- | :--- | :---: | :--- |
| **文集 / 短篇集 / 长篇** | 独立成篇、或章节数多（$\ge 20$ 章） | **委派模式** | 主 Agent 担任调度员（并发 $\le 3$），分派一次性子代理逐篇独立翻译。单批产量大，极致节省 Token。 |
| **中篇 / 强叙事作品** | 章节数少（$\le 15$ 章）且单一叙事弧 | **连续模式** | 主 Agent 在自身上下文内逐篇直译，天然保障前后人物口吻与上下文连贯。 |

---

## 三、单篇翻译 9 步执行标准闭环

无论是主 Agent 还是子代理，翻译每章必须严格执行 9 步闭环：

```
1. 读配置 ──> 2. 认领任务 ──> 3. 读源文 ──> 4. 双语翻译 ──> 5. 译员自检
                                                                   │
8. 记日志 <── 7. 双写 done <── 6. 自动化卡点校验 <─────────────────┘
    │
    └──> 9. 循环推进下一篇 (或触发跨书 Compact)
```

1. **读配置**：组合读取 `通用翻译引擎.md` + 选定 `领域配置.md` + `项目说明.md` + `术语表.md` + 黄金样本（既有译文对齐风格）+ 前篇末尾衔接上下文。
2. **认领任务**：读取项目 CSV 取首个 `todo`，状态改写为 `doing`（只写项目 CSV；根 CSV 为快照，由 `refresh_progress.py` 统一重建，不逐章写）。
3. **读源文**：读取 `原文/<章节>.md`。
4. **双语翻译**：
   - **成对标记**：原文置于 `===Original===` 块，译文置于 `===Chinese===` 块；
   - **标题合并**：所有标题单行合并为 `## English Title / 中文标题`，不拆两份；
   - **术语硬约束**：遇到术语表中的源词，必须采用指定译法，严禁自创同义词；
   - **大文件分段**：单章源文 $>50\text{ KB}$ 时，按自然场景/空行分段翻译后拼合。
5. **译员自检**：检查块对称性、无漏译、无截断、标题合并格式与术语一致性。
6. **自动化卡点校验**：
   ```bash
   python scripts/check_bilingual.py 译文/<章节>.zh-CN.md
   ```
   结构契约必须满足（退出码为 0 或仅含数字锚点误报）；若有格式或标记违规必须回退修复。
7. **回填状态**：项目 CSV 改写为 `done`（只写项目 CSV，单章即结；根 CSV 与 Plan.md 快照在批次结束时由 `refresh_progress.py` 统一重建）。
8. **追加日志**：在当前项目 `Plan.md` 追加单篇运行记录。
9. **推进循环**：继续取下一篇 `todo`。

---

## 四、跨书 Compact 机制（防上下文污染）

当一个项目所有 `todo` 变 `done` 后，在开启下一个项目前必须做一次上下文压缩：
- **保留**：批次调度指令、通用规则指针、全局进度指针；
- **丢弃**：上一本书的术语、译文片段、风格细节；
- **效果**：下一本书从干净的调度上下文起步，彻底防止术语与风格交叉污染。

---

## 五、产物与总控状态刷新

1. **译文结果文件**：`翻译项目/<项目名>/译文/<章节>.zh-CN.md`
2. **总控刷新**：完成批次翻译后运行：
   ```bash
   python scripts/refresh_progress.py --snapshot "YYYY-MM-DD，翻译批次" \
       --log-date YYYY-MM-DD --log-event "批次翻译" --log-done "+N" --log-notes "备注"
   ```

---

## 六、多批次并发与定时任务防重机制

当使用多个定时任务或并发 Agent（如批次 1、2、3）推进翻译时，必须杜绝争抢同一本书：
1. **空间分片（静态候选池互斥）**：各批次运行 `python scripts/scan_candidates.py --batch <1|2|3>` 获取专属候选，池间交集为 $\emptyset$。
2. **动态排他锁（认领.md）**：领书时写 `翻译项目/<项目名>/认领.md`（锁龄 < 48h 视为活跃），完工验收后删除锁文件（删锁流转）。
3. **提示词**：定时轮次任务用 `prompts/批量提示词/2_定时轮次翻译提示词.md`，手动直启用 `prompts/批量提示词/2_直启翻译提示词.md`；两者均为纯参数提示词，严禁携带书单、进度等状态——状态只认 CSV 与锁文件。
4. **调度与译写分离**：定时任务主 Agent 只做调度（摸底/上锁/派发/验收/删锁），一切翻译由一次性子代理逐章完成（并发 $\le 3$）；主 Agent 上下文严禁载入源文与译文正文，防止上下文膨胀引发截断中断。

---

## 七、定时轮次模式（cron 每小时循环）

批量翻译的常态运行方式：一条每小时触发的定时任务，固定批次号循环推进。运行时模型（2026-09-10 实测验证）：**一条定时任务绑定其创建会话，触发时将提示词作为新消息注入该会话；同一任务的所有轮次在会话内串行排队，不会并行**。不同任务（或手动会话 + 定时任务）之间可以真并行，须靠 §六 的锁与下述心跳让位互斥。

1. **专用会话**：定时任务必须在专门的新聊天中创建（运行时强制一会话一任务）；该会话即"夜班工位"，其内禁止混杂人工指令。用户日常会话不受任何打扰。
2. **纯参数提示词**：提示词只含批次号、翻译子代理类型、并发数、单轮预算四项；携带状态（书单/进度）的提示词在重复注入时必然引发重译冲突。
3. **并发池（滑动窗口）**：并发数 N 指同时在跑的翻译子代理上限。轮次内任一子代理完成且验收通过后，主 Agent 立即取下一个 todo 回填派发，保持满池；新一轮开局先清点仍在途的上轮遗留子代理（其章节处于 doing 且译文文件有新鲜心跳），其计数计入 N，只补足差额而不另起炉灶；当前认领书的 todo 不足以填满池时，按第 9 条选书策略追加认领下一本书（允许同批次同时持有多把认领锁，锁内均注明批次）。历史约束「同批次同时只锁一本／不开新书」在本模式下废除——N 设多少就跑满多少，直至预算耗尽或待译池清空。
4. **单轮预算纪律**：每轮预算 45 分钟（须小于触发间隔）。到点即停止回填新章，随后以**任务状态**为准等待在途子代理（非阻塞轮询）：完成即验收；仍在运行则继续等——超长章节合法耗时久（≤50KB 单次直出可达 30 分钟，流式分批者全程持续落盘），不设固定等待上限。仅当任务报错，或运行中且一切文件迹象静默超 30 分钟（单章最坏生成时长之上）才不再等待：后台任务不必杀死、任其自跑，保留 doing 断点收尾本轮（其若稍后自行落盘，由后续轮次摸底收编）——等同 [1308] 的收尾动作，doing 断点留给下一轮按本节第 6 条接力。等待期间下一轮提示词在会话队列排队，等完立即衔接，不丢轮。
5. **done 即结算**：任何一轮的摸底严禁重验、重译、重派已 done 章节；摸底只处理 doing 残留与 todo 派发。因此任意时刻注入 N 条重复提示词 = 恰好 1 轮有效工作 + N-1 次廉价空转，堆积无害。
6. **doing 断点三分支**（轮次交接协议）：处置前先判在途，避免与存活子代理同写一个文件——①本会话仍在运行的后台任务对应的 doing 章节即视为在途，跳过（任务句柄因上下文压缩丢失且译文从未落盘的同样跳过，连续两轮无产出后再按缺失处置）；②任务状态不可知时（冷启动/跨会话，此时原会话的子代理已随会话消亡）按文件心跳判断：译文 15 分钟内有写入说明存在其他并行执行流，按第 7 条心跳让位处理。其余 doing 按三分支处置：
   - 译文存在且 `check_bilingual.py` 通过 → 回填 done；
   - 译文存在但未通过 → 统计既有完整双语块（===Original===…===Chinese=== 成对闭合）覆盖的原文非空段数 N，文件尾部未闭合半块先截断，派断点续作子代理从第 N+1 段起 append-only 追加，整文件过检再置 done（若核验发现该章实际已完整，只补验收，不追加）；
   - 译文缺失 → 重置 todo 正常派发。
7. **心跳让位（跨会话互斥）**：轮次启动摸底时，若本批次认领书下最新译文落盘或 CSV 写入距今不足 15 分钟且非本轮所为，判定存在并行执行流（手动会话或其他任务），本轮输出一行快照后立即退出。同一任务内部天然串行，无需此规则。
8. **失败降级**：[1301] 内容拦截 → 原任务加学术文献免责前缀重派 1 次，仍失败保持 todo 自动跳过，严禁 2 次以上重试；[1308] 额度中断 → 保留 doing 断点不回滚，验收在途后停止新派发、收尾退出（若提示额度恢复在 30 分钟内可稍候再派一轮）。主 Agent 仅做 check 抽验与 CSV 无 doing 确认。
9. **选书与流转**：待译池优先小体量（<200KB）、低风险题材（小说/戏剧/儿童文学优先；宗教批判、冷战政论后置）；排除已有其他批次认领锁的书。整书全部 done 后：删认领锁 → 追加项目 Plan.md 运行日志 → `refresh_progress.py` 重建看板（若根 Plan.md 3 分钟内刚被其他执行流刷新，等 2 分钟再跑）→ 跨书 Compact（§四）→ 无 todo 可派时该轮直接退出并提示用户可删除本定时任务。
