# 阶段三：独立审核工作流（3_审核WORKFLOW.md）

本文件是**译文质量独立审校阶段的标准操作手册**。
贯彻**只读审核、不改译文、只出报告**的原则，由独立审校员对译稿进行整本精读核查。

---

## 一、角色与红线

1. **角色定位**：
   - **主 Agent**：审核调度员（维护台账、按体积由小到大分派子代理、核验报告并刷新总控）。
   - **子代理**：独立审校员（单子代理精读一本书，逐块对照中英，输出评估报告）。
2. **隔离红线**：
   - **绝对只读**：严禁修改 `译文/`、`原文/`、`术语表.md`、各项目 CSV、各项目 Plan.md。
   - **唯一输出**：只产出各书目录下的 `翻译项目/<项目名>/审核报告.md`，并通过 `python scripts/refresh_progress.py` 汇总至根目录 `Plan.md`。

---

## 二、可审门禁条件

必须同时满足以下三条才允许进入审核队列：
1. 项目 `translation_queue.csv` 中所有状态均为 `done`（0 todo、0 doing）；
2. `译文/` 目录下实际存在的 `.zh-CN.md` 数量 $\ge$ done 数；
3. 核心元数据完整：`项目说明.md`、`术语表.md`、`原文/`、`译文/` 均存在。

---

## 三、审校员 9 项精读核查清单

审校员对每章每个 `===Original===` / `===Chinese===` 块对逐项检查并评定问题级别：

| # | 检查项 | 判定标准 | 缺陷级别 |
|---|---|---|:---:|
| 1 | **语义错配** | 中文块内容与紧邻的英文块完全不对应（英文讲 A、中文讲 B） | **A 级（必须修）** |
| 2 | **整段漏译** | 中文块段落数少于英文块、整句或整段缺失未译 | **A 级（必须修）** |
| 3 | **章末截断** | 章节末尾半句缺失、末尾原文块无对应译文 | **A 级（必须修）** |
| 4 | **重大数字错译** | 年份、金额、度量、关键统计数据错误 | **A/B 级（按影响）** |
| 5 | **术语表违规** | 术语表列出的专有名词未按指定译法翻译 | **B 级（应修）** |
| 6 | **同名异译** | 同一书内同一人名/地名/核心概念跨章译法前后不一 | **B 级（应修）** |
| 7 | **英文单词残留** | 中文块中括号外出现非合法保留的英文词句 | **B 级（应修）** |
| 8 | **语义误译** | 通顺但含义颠倒（时态、人物主体、事件因果翻转） | **B/C 级（按影响）** |
| 9 | **风格与流畅度** | 存在明显机翻腔、语序生硬、文学性受损 | **C 级（建议优化）** |

> **合法保留不报错**：首现括注 `中文（English）`、外语原句保留附译（法语/拉丁语）、原诗行保留、技术专用词（如 linotype）。

---

## 四、执行步骤

1. **盘点待审队列**：扫描 `翻译项目/` 中符合可审门禁且尚未生成 `审核报告.md` 的项目，按原文体积升序排列（先小后大）。
2. **分派独立子代理（并发 $\le 3$）**：向子代理传入完整自包含简报（含项目说明、术语表、检查清单与报告模板）。
3. **核验报告落盘**：确认 `翻译项目/<项目名>/审核报告.md` 已生成、结构完整、包含质量等级评定与 A/B/C 问题统计。
4. **刷新总控看板**：运行 `python scripts/refresh_progress.py`，将审核等级、报告超链接与结论摘要自动同步到根目录 **`Plan.md`**「🔍 独立审校台账明细」与「📚 全生命周期主台账」。
