# 阶段四：终校与EPUB打包工作流（4_打包WORKFLOW.md）

本文件是**终校排版与双语 EPUB 格式化出版阶段的标准操作手册**。
将已通过阶段三审校的译稿进行靶向修复、按物理阅读序合并，并通过 Pandoc 编译构建为带排版样式的标准双语 EPUB 出版物。

---

## 一、强制质量门禁（Quality Gate）

> [!IMPORTANT]
> **硬性门禁**：严禁对未完成阶段三审核的书籍直接执行打包。
> 打包前必须确认 `翻译项目/<项目名>/审核报告.md` 存在且非空。打包的核心前置步骤是**以审核报告中记录的 A 级（必须修）与 B 级（应修）缺陷作为靶点进行终校修复**。

---

## 二、标准执行五步法

```
1. 审核缺陷终校修复 ──> 2. 双脚本逐章过检 ──> 3. 行序全书合并 (防乱序)
                                                       │
5. 交付终校报告与同步 <── 4. Pandoc 双语编译与校验 <──┘
```

### 1. 审核缺陷靶向修复
- 读取目标项目的 `审核报告.md`；
- 对报告中指出的 **A 级问题**（整段漏译、错配、章末截断）进行最小补译；
- 对 **B 级问题**（术语表偏差、同名异译、英文残留、明显错译）进行统一规整。

### 2. 双脚本逐章自动化过检
对修复后的每章译文运行校验：
```bash
python scripts/check_bilingual.py 翻译项目/<项目名>/译文/<章节>.zh-CN.md
python scripts/check_coverage.py 翻译项目/<项目名>/原文/<章节>.md 翻译项目/<项目名>/译文/<章节>.zh-CN.md
```
- `check_bilingual.py`：确保结构标记成对、标题单行合并；
- `check_coverage.py`：基于 20-gram 覆盖率拦截漏译。

### 3. 全书合并（章节物理顺序保障）
- **绝对防乱序机制**：**严禁按文件名排序合并**！必须严格按照 `translation_queue.csv` 的行物理顺序读取分章译文，合并输出至 `翻译项目/<项目名>/译文/<书名>.all.zh-CN.md`。

### 4. Pandoc EPUB 编译与校验
```bash
python scripts/bilingual_to_epub.py \
    翻译项目/<项目名>/译文/<书名>.all.zh-CN.md \
    翻译项目/<项目名>/<书名>.epub \
    --title "中文书名" --author "作者名"
```
- **排版规范**：英文原文无背景，中文译文自动注入浅黄色背景容器（`.bilingual-chinese`），字体适配所有标准阅读器。
- **完整性验证**：脚本自动校验 EPUB ZIP 包结构、mimetype 与样式表注入。

### 5. 产出终校报告与总控同步
- 将终校修改清单与打包参数写入 `翻译项目/<项目名>/终校报告.md`；
- 运行 `python scripts/refresh_progress.py`，将 EPUB 产物路径与大小自动同步至根目录 **`Plan.md`**「📦 EPUB 双语出版打包明细」与「📚 全生命周期主台账」。
