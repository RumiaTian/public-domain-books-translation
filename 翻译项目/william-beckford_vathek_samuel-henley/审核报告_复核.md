# 《瓦提克》（Vathek）独立复核报告（2026-09-21 旧方法论期批·盲审复核）

审校日期: 2026-09-21
质量等级：B 良好
一句话结论：素材与英文块逐字零损坏、术语基本落位，但正文有 1 处段内末三句整段漏译，另集成 3 类 B 级与 5 处 C 级瑕疵。

## 一、项目与核对范围

- 项目：`william-beckford_vathek_samuel-henley`（绝对路径 `C:\Users\HanTi\OneDrive\translate\翻译项目\william-beckford_vathek_samuel-henley`）
- 书籍：William Beckford《Vathek》1786，Samuel Henley 英译；底本为 Standard Ebooks 2026-08-17 revision `61c8bde` 版 epub（`原书/william-beckford_vathek_samuel-henley.epub`，653,671 字节）
- **章节数 / 块数 / 体量**（已排除干扰文件：`译文/` 下**不存在** `.all.zh-CN.md` 合并产物，无重复计数风险）

| 篇 | 原文 | 译文 | 块对 | 原文行 | EN 段 | CN 段 |
|---|---|---|---|---|---|---|
| `vathek` | 211,682 B / 210,307 字 | 396,979 B / 277,678 字 | 177 对 | 491 | 496 | 496 |
| `preface` | 920 B | 1,821 B | 1 对 | 2（含标题） | 1 | 1 |
| `endnotes` | 7,521 B | 14,762 B | 7 对 | 27（含标题） | 26 | 26 |
| **合计** | **220,123 B** | **413,562 B** | **185 对** | **520** | **523** | **523** |

- **阅读顺序及其核定依据**：以 `python scripts/epub_spine.py william-beckford_vathek_samuel-henley` 为权威，spine 内容页 3 篇（总 spine 8，其余为 SE 模板页）：
  1. `prologue.xhtml` → `Preface` → `原文/preface.md`
  2. `vathek.xhtml` → 正文 → `原文/vathek.md`
  3. `endnotes.xhtml` → `Endnotes` → `原文/endnotes.md`
  **陷阱**：文件名序（字母序）为 `endnotes → preface → vathek`，**与阅读序不一致**；三篇各自独立成篇、内部无交叉指代，实用影响为零，但归档/串联时须按 spine 排。
  **非缺陷的跳页**：epub 的 `halftitlepage / titlepage / imprint / uncopyright / colophon / toc` 为 Standard Ebooks 出版模板页（非作品内容），未被抽取，且 `toc.xhtml` 的 landmarks 只把 `vathek.xhtml` 标为 `bodymatter`。判为**正当排除**，非 R5 型 front/back matter 丧失。
- **已跑工具与命令**（均为项目外只读工具，输出已复算）：
  ```
  python C:\Users\HanTi\OneDrive\translate\scripts\epub_spine.py william-beckford_vathek_samuel-henley
  python ...\scripts\check_bilingual.py 译文\vathek.zh-CN.md   （preface / endnotes 同）
  python ...\scripts\check_source_coverage.py william-beckford_vathek_samuel-henley --min-chars 25
  python ...\scripts\check_chapter_ratio.py william-beckford_vathek_samuel-henley --min-ratio 0.20
  python ...\scripts\precheck_review.py william-beckford_vathek_samuel-henley
  ```
  另自建临时脚本（写在 `%TEMP%\vathek_review\`，未落项目目录）完成：epub 元素级对回、块/段三方计数、英文块逐字 diff、逐块专名落位、逐段译比扫描、字频/引号/FEFF 扫描。唯一写出的项目内文件为本报告。

## 二、数据层核对（程序化结论）

| 项 | 结果 |
|---|---|
| 块数 O=C | **通过**。`===Original===` 185 / `===Chinese===` 185（vathek 177+177、preface 1+1、endnotes 7+7），`check_bilingual.py` 三文件均报「标记成对：满足、标题合并格式：满足、可疑段落错配 0 处」 |
| 段数 原文/EN/CN 三方 | **通过（自洽）**。原文 520 行 / EN 块 523 段 / CN 块 523 段。差额来源已逐条落实：① `preface`、`endnotes` 的 `## 标题` 行各 1（原文含、块内不含，−2）；② `vathek` 原文 1 处单段被块内拆为 2 行共 5 处（+5）。**491+2+27 − 2 + 5 = 523**，无一处无法解释的缺口。**块内 EN≠CN 段数的块数 = 0**（177+1+7 全部对称） |
| 英文对照块逐字 diff | **零损坏（词级 0 处 / 字形档 0 处）**。`译文`信封的 EN 块串接后与 `原文/*.md` 行级 `difflib` 比对，仅 4 个 opcode、全部为**同一原文段被拆成两行**（` ' '` → `'\n'`），非字符改动：块 b117(EN#221/222)、b140(EN#277-280,2 处)、b147(EN#297/298)、b155(EN#409/410)。**无换词、无脱字、无破词、无中文串入、无整句覆盖**（对比本库历史判例 `steamer→ste轮`、`parlor→par丘`，本书干净） |
| 英文残留（剔合法保留） | **0 处违规**。CN 块内非括注英文仅：`p.` 14、`Herbelot` 9、`Habesci’s`+`State of the Ottoman Empire` 各 3、`Al Koran` 3、`ch.` 2、`Ginn` 2、`la daube` 1（均已局部译出：`按炖肉法（à la daube）`）。全部为尾注文献引证（Herbelot《东方文库》、Habesci、Sale、Al Koran 章号）与阿拉伯/法语术语音转，属**合法保留**。首现括注 `中文（English）` 共 60+ 项，亦合法 |
| 术语表硬约束落位 | **81 / 83 条完全落位，2 条定名漂移**（详见四、B-1/B-2）：`Morakanabad` 莫13/摩10；`sherbet` 果子露2/果汁露1。**预检的「落位偏低/译0次」全部复算为假阳性**，逐条以「源文词界次数 ↔ 译文词干次数」对账：`Circassians 译0`＝首现括注 `切尔克斯（Circassian）女奴` 打断子串（切尔克斯 3/源3）；`symar 2/3`＝`西玛尔（symar）长袍` 括注打断（西玛尔 3/源3）；`plain of Catoul 3/4`＝`卡图勒（Catoul）平原`（卡图勒 4/源4）；`Kaf 5/7`＝尾注 15 引证保留「从卡夫到卡夫」（卡夫山5＋卡夫7）；`Santons 2/9`＝正文用简称「圣僧」（苦修圣僧2＋圣僧9）；`Calenders 1/2`＝第二次作简称「卡连德尔僧」（卡连德尔2/源2）；`magi 2/6`＝`the religion of the Magi`→「祆教」为正确搭配（祆教祭司2＋祆教3，源文实有独立 `magi/Magi` 3 处全覆盖）；`Koran 9/12`＝3 处为尾注文献 `Al Koran ch. 72/ch. 4/ch. 27` 英文引证；`the Hall of Eblis`＝源文 **0 次**（见下行）而 `subterranean palace` 5 次→「地下宫殿」5 次，**完全落位** |
| 幽灵条目/归一残留 | **预检 3 条「源0次(幽灵?)」全部为预检假阳性**：实测源文 `sequins` ×1（`thousands of sequins of gold`）、`muezzins` ×2、`Deggial` ×1，三词均在原文中实有。真正的建库期赘项只有 1 处：术语表组合条目 `the Hall of Eblis / the subterranean palace` 中 **`the Hall of Eblis` 在全库源文词界出现 0 次**（后半 `the subterranean palace` 5 次且译文 5/5 落位），不构成硬约束。术语表末尾「候选专有名词」把 `Let`(18)/`Having`(18) 列为专名亦属自动提取噪声，非硬约束 |
| 原文素材覆盖（含 `--min-chars 25`） | **通过，0 段缺失**：`check_source_coverage.py … --min-chars 25` → `检查 514 段 (阈值25), 缺失 0 段`。**epub 元素级对回 100%**：`vathek.xhtml` 488 个 `<p>` ↔ 原文 491 行（多出 3 = 1 首四行诗按行拆分）；`endnotes.xhtml` 26 个 `<li>` ↔ 原文 26 条；`prologue.xhtml` 1 个 `<p>` ↔ `preface.md` 正文段，**逐一归一化后 100% 字符串相等**。R1–R5 排查结论见下 |
| 逐章译率（check_chapter_ratio） | **通过**：`vathek` 0.31（168,551 字母→52,435 汉字）、`endnotes` 0.29、`preface` 0.35，中位 0.31，**无一篇跌破 0.20**。另按**块/段**独立复算：逐段「汉字/英文字母」比中位 0.316、最低 0.151（即 A 级漏译处），逐块最低 0.151，无整章未译特征 |
| 异常字符 / 引号字形 | 弯引号 `“”‘’`、`「」` 完好，**无直引号归一、无 U+00A0/U+200A**。`U+200B` 0 处。`U+FEFF`：`vathek` 原文 16 = 译文 16（全在 EN 块，逐字保留，非缺陷）；`endnotes` 原文 15 → **中文块另引入 14 处**（`。—\ufeffHabesci’s` 形态），记 C 级字形档（见五、C-3）。无 `\u3000` 全角空格残留 |

### 专项⑥ epub 元素级对回（R1–R5 逐一排查结论）

| 代号 | 失效模式 | 本书排查结论 |
|---|---|---|
| R1 | 单 HTML 内多 `<article>`/多篇目只抽首篇 | **不适用**。`vathek.xhtml` 仅 1 个 `<section id="vathek">`、0 个 `<h2>`、0 个 `<article>`；`endnotes.xhtml` 仅 1 个 `<section>`／1 个 `<h2>`。书为单一叙事弧中篇，无选集结构 |
| R2 | `<blockquote>`/`<header>` 内文本未抽 | **已核，零缺口**。全库仅 1 个 `<blockquote>`（`vathek.xhtml` 内 Philomel 波斯诗四行，以 `<span>+<br/>` 承载）；其文本**完整进入** `原文/vathek.md`（拆为 4 行，L1540-L1549），译文 L1540-L1549 亦逐行译出。`uncopyright.xhtml` 的 `<blockquote>` 属 SE 模板页，不在抽取范围（正当排除） |
| R3 | `<img alt="…">` 公式/数学文本未抽 | **不适用**。`vathek.xhtml`／`endnotes.xhtml` 内 `<img>` 计数为 0（图片仅在 titlepage/imprint/colophon 模板页，alt 为 `The Standard Ebooks logo.`） |
| R4 | 剧本空说话人 `<td/>` 表格行未抽 | **不适用**。全库 `<table>`/`<tr>`/`<td>` 计数为 0（本书为第三人称小说，非剧本文本） |
| R5 | front/back matter 整页被跳过 | **零缺口**。逐页核：`prologue.xhtml`（`epub:type="frontmatter z3998:fiction"`，Standard Ebooks 会命中 `KEEP_FRONT` 漏词表）**已抽出**为 `原文/preface.md`；`endnotes.xhtml`（`backmatter endnotes`）**已抽出**为 `原文/endnotes.md`，26 条尾注一条不缺。被跳过的 `halftitlepage/titlepage/imprint/uncopyright/colophon` 均为 SE 出版模板页，`toc.xhtml` landmarks 亦不将其标为 frontmatter/rearmatter 内容页。**无 `preface/introduction/foreword/glossary/dramatis-personae/appendix` 类内容页遗漏** |

**判定**：五类素材失效模式**零命中**。`vdthek` 素材完整性达 epub 元素级 100%，不存在「源文有、交付物无」型的整节/整篇缺口。唯一漏译发生在**中文侧段内**（见三、A-1），成因在译写环节而非抽取环节。

## 三、A 级问题（必须修）—— 共 1 处

1. **`译文/vathek.zh-CN.md` L2763–L2777 块（第 161 块）第 3 段：段末三句整段无中文对应（38 词 / 175 字母 / 占该段 37.2%）。**
   - 定位：该块 `===Original===` 在 **L2763**；涉事英文段落在 **L2768**（第 3 个 EN 段）；对应中文段落在 **L2777**（第 3 个 CN 段）。
   - 英文块原文摘录（L2768 全文）：
     > They continued their way through the multitude, but notwithstanding their confidence at first, they were not sufficiently composed to examine with attention the various perspectives of halls, and of galleries, that opened on the right hand and left, which were all illuminated by torches and braziers, whose flames rose in pyramids, to the centre of the vault. **At length they came to a place where long curtains, brocaded with crimson and gold, fell from all parts, in striking confusion. Here the choirs and dances were heard no longer. The light which glimmered came from afar.**
   - 中文块译文摘录（L2777 全文，**止于第 1 句末**）：
     > 他们从人群中穿行而过；虽然起初满有把握，此时却心神不定，无暇细看左右两面展开的一重重厅堂与回廊——那里全靠火炬与火盆照着，火焰层层叠起如金字塔，直抵穹顶中央。
   - 下一块（第 162 块，中文 L2787）直接以「过了一阵，瓦提克和努罗尼哈望见帷幔后透出一团渐亮的光……」承接下个原文段，**未回收上述三句**。全文检索确认这些句子在中文侧**任何位置都不存在**（关键词 `合唱` 仅 1 次、`舞蹈` 2 次，均在无关段落）。
   - 问题定性：**段内整句群漏译**（叙事正文），符合九项清单 A-2「整段漏译」的 A 级口径，并属 B-4「叙事与戏剧正文缺失 → A 级」。段落计数门禁（EN=CN=3）与块数门禁（O=C）**均无法拦截**此类段内缺失——即简报所述「段数对称下的漏洞」。
   - 影响：丢掉由「满厅人群」过渡到「帷幔重重、乐舞俱寂、远处微光」这一场景转换，紧接下一段「望见帷幔后透出亮光」因此缺少铺垫。
   - **可复现命令**：
     ```bash
     grep -n "brocaded with crimson and gold" 原文/vathek.md 译文/vathek.zh-CN.md   # 原文命中；译文仅命中 EN 块那一行，中文块无对应
     grep -n "The light which glimmered came from afar" 原文/vathek.md 译文/vathek.zh-CN.md
     sed -n '2763p;2768p;2777p' 译文/vathek.zh-CN.md                                 # 块头 / 英文段 / 中文段
     ```
   - 处理：**挂起·待修**（补译该段末三句）。**不判 D**：不满足 B-5 任一条（a 段缺失 <10% 且段数完整；b 非整幕/整节/整篇；c 仅 1 处、非章末；d 非合集单篇结尾）。

## 四、B 级问题（应修）—— 共 3 类

**B-1（术语表违规／同名异译）`Morakanabad` 译名分裂：术语表定名「莫拉卡纳巴德」，译文另有 10 处作「摩拉卡纳巴德」。**

- 术语表（`术语表.md` L44）：`Morakanabad → 莫拉卡纳巴德`；原文 `Morakanabad` 共 23 次，译文 莫13 / 摩10。
- 10 处「摩」定位（`译文/vathek.zh-CN.md`）：**L665、L759、L795、L885（同行 2 次）、L965、L981、L983、L987、L1137**。
  - 英文块原文摘录（L664 / L2768 邻块对比）：`Morakanabad and his companions took it for some work...`（L664 原文段）→ 中文 L665「**摩**拉卡纳巴德和他的同伴们还只当……」
  - 中文块译文摘录（L239，正确形）：`哈里发依从了母亲的道理，召来首相**莫**拉卡纳巴德（Morakanabad），说道：`
- 定性：同一人物跨段两种汉字，属九项清单 #5 术语表违规 + #6 同名异译叠加。分布呈「前段用莫、后段部分改摩」的**分段委派接缝特征**（与 `项目说明.md` 备注所述「分 3-4 段委派子代理」一致），但按 A 级统一口径「正文与标题落位不适用单一术语漂移豁免」，计 B 级。
- **可复现命令**：`grep -n "摩拉卡纳巴德" 译文/vathek.zh-CN.md`（10 行命中）；`grep -c "莫拉卡纳巴德" 译文/vathek.zh-CN.md`（13）

**B-2（术语表违规）`sherbet` 译名与术语表不符：术语表定名「果汁露」，译文 2 处作「果子露」。**

- 术语表（`术语表.md` L72）：`sherbet → 果汁露`；原文 3 次，译文 果子露2（**L943**、**L1437**）/ 果汁露1（L2011）。
  - 英文块原文摘录：L934 `flagons of wine, and vases of exquisite sherbet, floating on snow.` → 中文 L943「一坛坛美酒，一瓶瓶上乘的**果子露**，浮在雪上。」
  - 英文块原文摘录：L1432 `as many vases of the same material filled with excellent sherbet sparkled on a large table` → 中文 L1437「同样质地的千百只瓶盏盛着上好的**果子露**（sherbet）」
  - 首现（L943）**未加原文括注**且用「果子露」，后两处一「果子露」一「果汁露」——三处三种处理。
- **可复现命令**：`grep -n "果子露" 译文/vathek.zh-CN.md`；`grep -n "果汁露" 译文/vathek.zh-CN.md`

**B-3（引注体系损伤）尾注编号 1–10 在中文块全部缺失，而 11–26 全部保留，形成前后不一致。**

- 英文块内尾注标记为 epub `<a epub:type="noteref">` 内联数字，完整保留在 EN 块；中文块**丢弃了第 1–10 号标记**（对应 `endnotes.md` 第 1–10 条），保留 11–26。
- 缺失清单（EN 块行号 → CN 块行号，均为 `译文/vathek.zh-CN.md`）：

| 标记 | EN 块出处 | CN 块位置 | 中文现状 |
|---|---|---|---|
| 1 | L4 `Vathek, ninth Caliph1 of the race…` | L13 | 「……第九代哈里发——是穆塔西姆之子」（无编号） |
| 2 | L4 `one of his eyes became so terrible2` | L13 | 无编号 |
| 3 | L6 `the Caliph Omar Ben Abdalaziz,3` | L15 | 无编号 |
| 4 | L8 `which commanded the whole city of Samarah,4` | L17 | 无编号 |
| 5 | L44 `beautiful as the Houris,5` | L53 | 「个个美如天女（Houris），妖娆也不稍逊」（无编号） |
| 6 | L64 `said he to the Genii,6` | L71 | 无编号 |
| 7 | L396 `Accursed Giaour!7` | L405 | 「该咒的吉奥尔（the Giaour）！」（无编号） |
| 8 | L466 `whisper to Bababalouk,8` | L475 | 无编号 |
| 9 | L470 `the hour of the divan9 sounded` | L479 | 无编号 |
| 10 | L486 `the prime vizier10 perceiving` | L495 | 无编号 |

- 对照：11–26 **保留**为裸阿拉伯数字并附尾随空格，例如 L947「……吉安·本·吉安（Gian Ben Gian）**11** 的王冠」、L985「……罗克纳巴德（Rocnabad）**12** 的溪水」、L2787「……怪兽乌拉纳巴德（Ouranabad）**26**」。
- 定性：`endnotes.zh-CN.md` **26 条尾注一条不少、逐条译出**（见二、素材覆盖），故非附录缺失；缺陷是**中文正文侧的注释定位信息丢失 10 处**，读者无法把尾注 1–10 与正文位置对应。按 B-4，属文献附录类材料的**指代/定位损伤**，判 **B 级（应修）**，不进「必须修」清单。
- **可复现命令**：
  ```bash
  grep -n "Caliph1 of the race" 译文/vathek.zh-CN.md      # EN 块保留标记 1
  grep -n "第九代哈里发" 译文/vathek.zh-CN.md              # CN 块无标记
  grep -n "Gian Ben Gian）11" 译文/vathek.zh-CN.md         # 11 起保留
  ```

## 五、C 级问题（建议优化）—— 共 5 处（合并计数）

- **C-1 标点：中文段缺后引号 1 处。** `译文/vathek.zh-CN.md` **L741**：「“把这飞来横祸都算在我头上，可真是妙事，”他说，“你们的孩子玩耍时从原先这儿的悬崖上跌了下去；若不是我猛地向后一跳捡回一条命，也早遭了他们的下场。**（缺 ”）** 对应英文段 L740 以 `…saved by a sudden start back.”` 收尾并闭合引号。全库唯一一处中英引号起止不匹配（另两处 L33 嵌套书名号、L2447 跨段引语均为合法）。复现：`sed -n '741p' 译文/vathek.zh-CN.md`。
- **C-2 术语落位不严：`prime vizier` 2 处译作「首相」而术语表为 `vizier → 维齐尔`。** L239（原文 L232 `sending for Morakanabad, his prime vizier`）、L495（原文 L486 `the prime vizier10`）。同一人物在别处 17 次作「维齐尔」，读来无歧义，故记 C 级而非 B；若派发方按硬约束计，亦仅 1 类、可升级为 B。
- **C-3 字形档：`endnotes.zh-CN.md` 中文块引入 14 处 `U+FEFF`（ZWNBSP）。** 表现形态为「。—**\ufeff**Herbelot, p. 690」；`原文/endnotes.md` 仅 15 处（全在英文原文），中文块另增 14 处。按 B-1 口径属写盘管线字形副产品，**记 C 级、单列计数，不据此下调总等级**。复现：`grep -c $'\xef\xbb\xbf' 译文/endnotes.zh-CN.md`（29，含英文原文侧 15）。
- **C-4 缺宾语（语义可复原）：`译文/vathek.zh-CN.md` L1881**「他们正为丢了绝望已极，已经派了太监们朝四面八方去找她。」——「为丢了」后脱宾语「她」（原文 L1874 `who, in despair at having lost her, had despatched eunuchs to seek her in every direction.`）。后一小句已补出「她」，语义可复原，故不判 A/B。
- **C-5 句法重心偏移（语义可复原）：`译文/vathek.zh-CN.md` L91**「他的任何臣民，若敢触怒天威，不得收留或滞留任何旅客，而应即刻将其护送入宫。」——原文 L82 `that no one of his subjects, on peril of his displeasure, should either lodge or detain a traveller, but forthwith bring him to the palace.` 把「on peril of his displeasure」（违令即遭严惩的状语）改写成条件从句「若敢触怒天威」，禁令与惩罚的因果被前置，语势略偏但语义可复原。

## 六、逐项清单结论

| # | 检查项 | 结论 | 缺陷数 |
|---|---|---|---|
| 1 | 语义错配 | 无。逐块 EN→CN 语义对齐一致；`check_bilingual.py` 三文件「可疑段落错配 0 处」；逐块专名落位扫描未发现块内讲 A 译 B | 0 |
| 2 | 整段漏译 | **有**。第 161 块第 3 段末三句（38 词）无译文 → A 级 | 1（A） |
| 3 | 章末截断 | 无。末块（第 177 块）中文止于 L3000「……度过了整整一个个世代。」，与原文 L491 末段（`Thus the Caliph Vathek…the pure happiness of childhood.`）逐句对应；三篇均无末尾缺句 | 0 |
| 4 | 重大数字错译 | 无。24 个含数字块逐项复核：一万一千级台阶（L89/L925）、三百道菜尝三十二道（L167）、五十二名奴隶＋同数杏子（L241）、五十个孩子（L621/L641/L735）、一百四十人（L885）、三十六张脸（L1123）、六里格（L1155）、一百二十峰骆驼（L1189）、第九百九十九遍（L1449）、两千座坟（L2349）、十三万杂色马（尾注4）、一万二千金座（L2842）、六尺身长（L1867）——全部与原文一致；`六万`/`六十里格` 等亦对 | 0 |
| 5 | 术语表违规 | **有**。`Morakanabad`（B-1，10 处）、`sherbet`（B-2，2 处）；`prime vizier→首相`（C-2，2 处）。硬约束 83 条中 **81 条定名完全落位**（含 9 条预检误报的假阳性已逐条对账），组合条目 `the Hall of Eblis` 为源文 0 次赘项 | 2 类 B ＋ 1 类 C |
| 6 | 同名异译 | **有**。`Morakanabad` 莫/摩（B-1）；其余 60+ 专名经「形近字宽松正则」（如 `[莫摩模]拉[卡咖喀][纳娜]巴[德得]`）全量扫查**无第二组变体** | 1 类 |
| 7 | 英文单词残留 | 无违规。详见二·英文残留行；60+ 首现括注、尾注文献引证、`à la daube` 均合法 | 0 |
| 8 | 语义误译 | 无颠倒型误译。C-4/C-5 两处为可复原的句法/成分瑕疵 | 0（C×2） |
| 9 | 风格与流畅度 | 译文文学性佳、无机翻腔；C-4 一处断句不畅、C-1 一处标点 | 2（C） |
| 专项① | 英文对照块逐字损坏 | **零处**（词级 0 / 字形档 0）；仅 5 处原文单段被拆为两行 | 0 |
| 专项② | 段数三方对齐 | 通过。491→496(+5 换行拆段)、520↔523 差额全部落实；块内 EN=CN 的块数 177/177+1/1+7/7 | 0 |
| 专项③ | 双侧同缺 | **命中 1 处**（A-1）：块内 EN=CN=3 完全对称，第 3 段末三句 EN/CN 双侧同时缺损，段数与块数门禁均拦不住。已用「逐段汉字/英文字母比」扫描全 494 段（中位 0.316），仅此段 0.151 显著离群，其余 7 段低比值经逐一目视确认为中文简练所致 | 1（A） |
| 专项④ | 夹带与幽灵内容 | 无。EN 块与原文逐字一致（不存在英文幽灵残句）；CN 块无章末重复对话、无杜撰署名块；术语表 3 条「源 0 次」经复算为预检假阳性 | 0 |
| 专项⑤ | IME 错字扫描 | 未发现形近别字（无「个月人」/「孟妄」同族）。`摩`/`莫` 一处属译名分裂（B-1）而非错字；`果子露`/`果汁露` 属术语漂移（B-2） | 0 |
| 专项⑥ | 原文素材完整性（R1–R5） | **零命中**。epub 元素级对回 100%（488/488 `<p>`、26/26 `<li>`、1/1 preface `<p>`）；`<blockquote>` 唯一 1 处已抽；无 img alt / 无表格 / 无多 article；front matter 已抽；SE 模板页为正当排除 | 0 |
| 专项⑦ | 逐章译率核对 | 通过。vathek 0.31 / endnotes 0.29 / preface 0.35，无一篇跌破 0.20；最低单段比 0.151 即 A-1 | 0 |

## 七、等级判定理由

**判 `B 良好`。** 对应本库判据：「B 良好：无 A 级问题但 B 级成片/系统性；**或有 1–2 处 A 级而整体质量仍过硬**」。

1. **有 1 处 A 级**（第 161 块段内末三句漏译，叙事正文，38 词 / 175 字母 / 占该段 37.2%），落在「1–2 处 A 级」区间，未达 `C 需关注` 所需的「A 级若干」。
2. **不判 D 严重**：`B-5` 四条判据逐条不成立——a) 段落缺失比例 <10%（段数完整 523=523，仅段内缺句）；b) 不构成整幕/整节/整篇；c) 非「≥2 处章末/终场缺失」（本书章末、篇末均完整）；d) 非合集单篇结尾缺失。按 2026-09-21 裁决口径「不满足 a–d 的零星叙事缺失：记 A 级、总等级按 A 级数量落 B/C，**但始终须标『挂起·待修』**」——本报告已在三、A-1 标注修复点与复算命令。
3. **数据层五项中 4 项全清**：块段三方零不对称 ✔、英文块**词级**逐字一致 ✔、英文残留 0 ✔、数字全对 ✔；惟**术语非零违规**（2 类），故不满足 `A 优秀` 的「术语零违规」硬条件；且 A 级 1 处本身已排除 A。
4. **整体质量过硬**：素材 epub 元素级 100% 完整、英文对照块零损坏、译率正常无整章未译、无章末截断、无夹带、文学性与可读性在已复核各书中居前。B 级仅 3 类（2 类术语 + 1 类引注），未形成「跨章异译十几组」或「系统性器物误译」的成群之势。
5. **两可情形（供派发方裁量）**：
   - **C-2「首相」**：若按 A 级统一口径把 `vizier` 硬约束解释为「任何含 vizier 的搭配都必须用『维齐尔』」，则 C-2 升为 B 级，B 级由 3 类变 4 类——**总等级仍为 B 良好，不改变结论**；本报告按「复合词 prime vizier 有独立通行译法『首相』、且同句已给出人名」的读法记 C。
   - **B-3 尾注编号**：若派发方认为「编号丢失不影响尾注本身可读性」，可降为 C 级；本报告按「正文指代定位信息 10 处丢失、且 11–26 保留形成不一致」记 B。
   - **A-1 定级**：可读作「整段漏译（A 行 2）」或「段内严重脱译（A 行 1）」，**两种读法均为 A 级，不影响总等级**。

## 八、元数据备注（不改文件，仅提示人工）

1. **阅读序陷阱（须修档）**：spine 序为 `preface → vathek → endnotes`，而 `原文/`、`译文/` 的文件名字母序为 `endnotes → preface → vathek`，二者不一致。三篇独立成篇、无内部互指，实用影响小，但任何按文件名串联的脚本都会读错顺序。
2. **`项目说明.md` 有一处需修正**：「内容形态：中篇……（3 篇，约 215KB）」——实测 `原文/` 三篇合计 **220,123 字节**（vathek 211,682 + endnotes 7,521 + preface 920）。数量级一致，表述「215KB」偏小，建议改为「约 215KB（vathek 单篇 207KB）」或直接写 220KB。
3. **`项目说明.md` 的元数据前提被复核纠正一处**：`项目说明.md` 与 `预检` 摘要称「preface/endnotes 独立成篇」，与 spine 一致（更正为：preface 在 epub 中定名为 `prologue.xhtml`，`epub:type="frontmatter z3998:fiction"`，属 R5 高发形态，本书**已正确抽出**，非缺口）。
4. **分段委派接缝可佐证**：`项目说明.md` 备注「按通用翻译引擎第七节分 3-4 段，委派子代理在独立上下文完成；连贯性靠术语表与分段衔接」。本次复核的 3 类缺陷（尾注编号 1–10 丢失、`Morakanabad` 莫/摩分裂、`sherbet` 三种处理）**全部呈现分段接缝特征**，可作为「分段委派缺乏跨段一致性回校」这一流程风险的实证样本。
5. **术语表建库噪声**：① 组合条目 `the Hall of Eblis / the subterranean palace` 的前半 **`the Hall of Eblis` 在全库源文出现 0 次**（本书只写 `the subterranean palace`／`the palace of subterranean fire`），属建库期赘项，勿据此判译文违规；② 末尾「候选专有名词（自动提取）」把 `Let`(18)、`Having`(18) 列为专有名词，属自动提取误植；③ `sequin`/`muezzin`/`Deggial` 三条在预检中报「源×0（幽灵?）」系**预检假阳性**（原文实有 1/2/1 次），**勿据此删表**。
6. **主篇缺标题行**：`原文/vathek.md` 与 `译文/vathek.zh-CN.md` 均为 0 个 `#` 标题行（epub 的 `vathek.xhtml` 本身无 `<h2>`，书名在独立的 `halftitlepage.xhtml`）。对照 `preface`/`endnotes` 两篇均有 `## 篇名 / 译名` 合并标题行——**同项目内标题口径不统一**，且交付物正文篇无书名/副标题（`An Arabian Tale`）落位。属格式口径问题，建议统一补 `## Vathek / 瓦提克`。
7. **未发现干扰文件**：`译文/` 下无 `*.all.zh-CN.md` 合并产物，块/段计数无翻倍风险；亦无附录、题注等非章节文件混入。

## 九、复核方法与局限

**读了什么（全部为盲审允许范围）**：`原书/william-beckford_vathek_samuel-henley.epub`（解包后逐字节读取 `content.opf`、`toc.ncx`、`toc.xhtml`、8 个 xhtml 内容页）；`原文/vathek.md`、`原文/preface.md`、`原文/endnotes.md`；`译文/vathek.zh-CN.md`（全 3,001 行逐行通读）、`译文/preface.zh-CN.md`、`译文/endnotes.zh-CN.md`（全文通读）；`术语表.md`、`项目说明.md`。**未读取**本项目旧 `审核报告.md`、`Plan.md`、`translation_queue.csv`、任何批次记录或统计文件，未读取任何其他项目文件。

**方法与取证**（临时脚本全部置于 `%TEMP%\vathek_review\`，项目目录只写报告一个文件）：
1. **epub 元素级对回**：正则抽取 `<p>`/`<li>` 文本 → `html.unescape` → 去 `\u200b\ufeff` → 空白归一 → 与 `原文/*.md` 逐串相等比对；并统计全库 `<article>/<h2>/<blockquote>/<header>/<img>/<table>/<tr>/<td>` 计数以判定 R1–R5 适用性；另对 body 做「剥除 `<p>` 后剩余可见文本」检查，结果为**空串**，证明 `vathek.xhtml` 正文无 p 外文本。
2. **块/段三方计数**：按 `===Original===`/`===Chinese===` 状态机解析，逐块记录段数与行号；对差额做「标题行 / 换行拆段 / 诗行拆分」逐条归因，直至 523=523 自洽。
3. **英文块逐字 diff**：`difflib.SequenceMatcher` 行级 + 差异窗口内字符级二级 diff，区分「整字换词（词级档）」与「空白/字形（字形档）」。
4. **专项①判定**：以 `原文/*.md` 为基准，比对范围为块内 EN 段串接串，报告 opcode 数与类型（结论零损坏）。
5. **双侧同缺（专项③）**：三重独立信号交叉——(a) 逐块 EN/CN 段数；(b) 逐段 `汉字数 / 英文字母数` 比值（中位 0.316，离群阈值 0.25 且 en≥120，共 8 段，逐一目视）；(c) 逐块「英文块出现的专名/数字必须在同块中文块有对应译名/数字」的白名单扫描（命中 14 块，逐块目视后全部为 `magi` 匹配 `magic/imagination` 的子串假阳性，以及 `harem→妃嫔/女眷`、`seraglio→内院` 等合法借代）。
6. **术语落位**：`术语表.md` 83 条硬约束逐条对「源文出现次数 / 译文定名次数」复算；再对 60+ 专名做形近字宽松正则（如 `[莫摩模]拉[卡咖喀][纳纳娜]巴[德得]`）全量扫查以捕获未预料变体。
7. **引号/字符**：逐行引号首尾配对与 EN 段对照；`U+FEFF/U+200B/U+00A0/U+3000` 逐文件计数并与原文对账；低频汉字（≤2 次，906 字）全量列出供人工扫读。

**局限（如实声明）**：
- **段内漏译的检出为阈值型，非穷尽**。上述 (b)(c) 两路信号对「单句缺失、长度占段 20%–25%」的情形灵敏度不足（本次漏译为 37.2%，落在可检出区）。已用人工通读全 3,001 行全文复核，未再发现其他段内缺口，但**不能排除更小粒度（半句、短语级）遗漏**；建议在 A-1 修复时由译者顺带全文回校一次。
- **中文侧形近字/别字扫描为人工通读 + 低频字清单目视**，未使用语料级别字模型；本报告声明「未发现」不等于数学意义上的零。
- **`check_source_coverage.py`（514 段 0 缺失）与自建元素级对回为同向双证据**，但两者都以「原文 md ↔ epub」为对象，无法发现「epub 本身即缺页」的底本级问题；底本完整性以 Standard Ebooks `toc.ncx`/`content.opf` 为唯一依据，未获取纸质本或其他版本对校。
- **未评估内容层面的史实/名物准确性**（如 Habesci、Herbelot 文献引证是否与真实著作页码相符），该层面超出纯文本比对范围。
- 预检 19 条「残留/落位/幽灵」告警中，**复算后 19 条全部为子串/括注/搭配/变体口径假阳性**，仅 `sherbet 落位偏低 1/3` 一条经回读核实为真缺陷（B-2）。

**结论**：素材与英文对照层近乎完美（epub 元素级 100%、英文块零损坏、数字零错），中文侧有 1 处 A 级段内漏译（**挂起·待修**，须补译 `译文/vathek.zh-CN.md` L2768 末三句）与 3 类 B 级（Morakanabad 译名分裂 10 处、sherbet 译名漂移 2 处、尾注编号 1–10 缺失 10 处）、5 处 C 级。总等级 **B 良好**。
