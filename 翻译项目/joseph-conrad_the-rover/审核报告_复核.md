# 《漫游者》（The Rover）独立复核报告（2026-08-14 批次）

审校日期: 2026-09-16
质量等级：B 良好
一句话结论：全书中译精准流畅、康拉德式绵长句法大体保住，仅 chapter-12 一处整段双侧同缺（A 级）与 10 类 B 级瑕疵，源书题词/献词未抽取。

## 一、项目与核对范围

- 项目：`joseph-conrad_the_rover`（**实际目录名为 `joseph-conrad_the-rover`，连字符**；见第八节元数据备注）
- 结构：16 章，与 epub spine 一致；无分片（无 `_partN`）、无 `译文/*.all.zh-CN.md` 合并产物干扰
- 体量：
  - `原文/*.md` 16 个文件，479,026 字符（含 markdown 强调星号）
  - `译文/*.zh-CN.md` 16 个文件，667,117 字符；其中 `===Original===` 块 475,114 字符、`===Chinese===` 块 165,137 字符（汉字 140,152）
  - 块对：`===Original===` 808 / `===Chinese===` 808（每篇 1:1，无跨块错位）
- 阅读顺序核定：以 `python scripts/epub_spine.py joseph-conrad_the-rover` 为准 —— spine 内容项 18（`epigraph`、`dedication`、`chapter-1`…`chapter-16`），总 spine 项 23。
  **陷阱**：文件名 `chapter-1.md … chapter-16.md` 的字典序会把 `chapter-10` 排到 `chapter-2` 之前，实际阅读序为数字序 1→16；本报告全部统计与逐章核对均按数字序，且已显式排除 `## I / 第一章` 之类的章题行与 `---` 分隔线，不计入中文段数。
- 已跑工具与命令：
  - `python scripts/epub_spine.py joseph-conrad_the_rover`
  - `python scripts/check_source_coverage.py joseph-conrad_the_rover --min-chars 25`
  - `python scripts/check_chapter_ratio.py joseph-conrad_the_rover --min-ratio 0.20`
  - `python scripts/check_bilingual.py <每个 译文/*.zh-CN.md>`
  - 自写只读脚本（置于项目外 `translate/_tmp_rover_*.py`）：英文对照块逐字归一化 diff、原文/EN/CN 段数三方对齐、epub xhtml↔`原文/*.md` 段级比对、术语落位计数、引号字形/星号/拉丁残留扫描

## 二、数据层核对（程序化结论）

| 项 | 结果 |
|---|---|
| 块数 O=C | **808 = 808**，逐篇 1:1，无跨块错位、无块标记损坏 |
| 段数 原文/EN/CN 三方 | **1324 / 1324 / 1327**。差异三处已逐一定位：ch11 原文 1 条 `---` 分隔线未进块对（计数口径，非缺陷）；**ch12 原文 1 段在 EN 与 CN 双侧同缺（真缺陷，A 级）**；ch15 EN +2（1 段被拆成 3 段）、CN 再 +3（法文歌谣 3 行逐行附中译，合法拆分/附译）。除 ch15 外无任何块出现 EN/CN 段数不等 |
| 英文对照块逐字 diff | 字形档：源文 U+FEFF 406 处 / EN 块 169 处、U+200A 细空格源 124 / 块 61（归一化不彻底、逐章不一致）；`*强调*` 星号 source→block 删除 20 处、block 新增 6 处；ch10 EN 块 4 处把 `. …` 写成 `……`。词级档：**4 处**（详见三/四节）。无中文汉字窜入 EN 块（CJK 码点计数 = 0） |
| 英文残留（剔合法保留） | **0 处违规**。剔除括注与 `*…*` 后的拉丁串仅 20 处，全部为合法保留：`Testa Dura`/`Poigne-de-Fer`/`la belle France`/`Adieu`/`Steady`/`E. Réal`/`E.A.`/`S.S.E.`/`Mademoiselle Catherine` 等专名与外语原句 |
| 术语表硬约束落位 | 98 条硬约束行中 93 条正常（译次≈源次）。3 条异常：`presbytery` 违规（见 B3）、`ci-devant` 漂移（见 B4）、`Petite Passe/the Passe` 多形（见 B5）。其余「落位偏低」为预检口径误报（见第八节） |
| 幽灵条目/归一残留 | 未发现幽灵条目。预检 `★源0次` 的 `Allons—du courage` **实测源文存在**（ch11：`*Allons—du courage*`，因 `Allons` 与 `—` 之间夹 U+FEFF/U+200A 导致精确匹配失败）→ 非幽灵。预检「残留」列（漫游者×67 / 公民塞沃拉×20 / 阿米莉亚号×59 / 苏珀布号×2）均为译文自身用词，非违规 |
| 原文素材覆盖（`--min-chars 25`） | 报 **缺失 2 段** = `epigraph.xhtml` 的 2 行斯宾塞题诗。经 epub spine 复核：`dedication.xhtml`（献词整页）同样未抽取，因各段短于 25 字符被阈值过滤，**预检漏报**。逐章 `原文/*.md` ↔ `chapter-N.xhtml` 段级比对 **16/16 一致**（ch15 的唯一差异是 epub 把 3 行歌谣并进一个 `<p>`） |
| 异常字符 / 引号字形 | **引号三体系并存**：ch2、ch11、ch13 全篇直引号 `"`；ch3 全篇直角引号 `「」`；其余 13 篇弯引号 `“”`（见 C1）。`\uFEFF`/`\u00A0`/`\u200B` 残留仅出现在 `原文/` 与 `===Original===` 侧（写盘归一化产物），中文侧干净。1 处中文对话引号未闭合（ch5，见 B7） |

## 三、A 级问题（必须修）—— 共 1 处

1. **chapter-12 整段「双侧同缺」——英文对照块与中文块同时丢失源文一整段（专项③本库 A 级主形态）**
   - 文件：`译文/chapter-12.zh-CN.md`，第 15 组块对（`===Original===` 第 15 次出现，自 1 计数）
   - 英文块原文摘录（该段开头，整段 1,862 字符 / **353 词**，全文不存在于译文任何一侧）：
     > "It was as if somebody had let off a lot of fireworks in his brain. He was illuminated, dazzled, confused, with a hissing in his ears and showers of sparks before his eyes. When he raised his head he saw he was alone. Peyrol had vanished. …"
     段末："> … To rob him of his right to Arlette was part of the conspiracy, no doubt."
   - 中文块译文摘录（该段的上一句与下一句直接相接，中间被跳过）：
     > "……可雷亚尔傍晚为什么不跟佩罗尔一道上来呢？塞沃拉问自己，坐在长凳上，两手攥在膝盖之间。……“这是他们的狡猾，”他忽然断定，“密谋的人总是避免让人瞧见他们待在一块儿。哈！”"
   - 复现：`grep -n "let off a lot of fireworks" 译文/chapter-12.zh-CN.md` → 无结果；`grep -n "let off a lot of fireworks" 原文/chapter-12.md` → 命中；`grep -n "showers of sparks\|three of the lamp flames\|deck of a privateer\|looking through the panes" 译文/chapter-12.zh-CN.md` 全部无结果。
   - 定性：段数三方对齐显示 ch12 原文 71 段 / EN 70 段 / CN 70 段，缺口恰为 1 段且**两侧同步缺失**，成因在块准备/回填环节（非译者漏译单侧），但读者确实读不到这段情节（佩罗尔熄灭三盏灯芯留在正厅过夜、塞沃拉隔窗窥视、塞沃拉「密谋」心理再确认）→ 按专项③定 **A 级**。
   - 规模判定（B-5）：1 段 / 1,324 段 = 0.08%（不满足 a），非整幕整节整篇（不满足 b、d），仅此 1 处非章末（不满足 c）→ **不判 D**，记 A 级，总等级按 A 级数量落 B。

## 四、B 级问题（应修）—— 共 10 条

1. **chapter-5 两段对话次序颠倒（EN 块与 CN 块同步错位）**。源文与 epub 次序为「博尔特请求褒奖」→「舰长：Yes. You acted prudently…」→「博尔特：I was afraid of spoiling our chances…」；译文块次序改为「请求」→「I was afraid…」→「Yes. You acted prudently…」，导致舰长的肯定答复跟着错位的上一句。
   - 复现：`grep -n "I was afraid of spoiling our chances" 译文/chapter-5.zh-CN.md`（出现在 `“Yes. You acted prudently by not advancing inland,”` 之前）；对照 `原文/chapter-5.md` 中两者顺序相反。
2. **英文对照块词级损坏 4 处**（语义中性，但属 B-1 词级档，逐处列明）：
   - ch9：`being woken by the thump`（原文与 epub 均为 `being wakened by the thump`）
   - ch12：`He had his own view on the patriot's psychology`（原文 `his own view of the patriot's psychology`）
   - ch15：`as though he possessed a cultivated sense of incongruity`（原文 `as though he had possessed`，脱助动词 `had`）
   - ch15：`risk damage to his rigging or hull`（原文 `risk damage in his rigging or hull`）
   - 复现：`grep -n "being woken by the thump\|view on the patriot" 译文/*.zh-CN.md`；`grep -n "he possessed a cultivated sense\|damage to his rigging" 译文/chapter-15.zh-CN.md`
3. **术语表违规：`presbytery → 神父住宅` 6 处中 5 处未按定名**：`神父寓所（presbytery）`×4（ch10）、`神父宅里的神父`×1（ch4），仅 ch11 一处作 `神父住宅`。
   - 复现：`grep -c "神父寓所" 译文/chapter-10.zh-CN.md` → 4；`grep -n "神父宅里" 译文/chapter-4.zh-CN.md`
4. **术语表违规：`ci-devant → 前贵族` 6 处中 1 处作「旧贵族」**（ch6：`雷亚尔是一对旧贵族（ci-devant）夫妇的儿子`），其余 5 处合表。
   - 复现：`grep -n "旧贵族" 译文/*.zh-CN.md`
5. **专名多形（同名异译）：同一条水道 `Petite Passe / the Passe` 三种译名**——`帕斯水道`×6（ch6 等）、`小海峡`×6（ch3/ch5/ch4）、`小水道`×3（ch13/ch15），且首现括注也不统一。
   - 复现：`grep -c "帕斯水道" 译文/*.zh-CN.md`、`grep -c "小水道" 译文/*.zh-CN.md`
6. **同一人称谓不一致**：ch1 的港口办事处独眼军官，前文作「某位海军上校」，同一段末又作「好管闲事的上尉」；源文两处均为 `captain`（`a certain naval captain with a patch over one eye` / `the inquisitive captain with the patch over one eye`）。
   - 复现：`grep -n "海军上校\|好管闲事的上尉" 译文/chapter-1.zh-CN.md`
7. **中文对话引号未闭合 1 处**：ch5 中文块 `“可也未见得比你那个女人更玄。我想，你确是看见了什么实在的东西……`（段末缺后引号 `”`；同段 `“`=2 而 `”`=1）。源文该处有闭合引号。
   - 复现：`grep -n "你确是看见了什么实在的东西" 译文/chapter-5.zh-CN.md`
8. **中文错字/脱字 2 处**：
   - ch16：`究竟有几分是干真万确的实情` → 应为「**千**真万确」（形近字，IME 类）— `grep -n "干真万确" 译文/chapter-16.zh-CN.md`
   - ch5：`要等许多之后，他做了俘虏` → 脱「年」，句子不成立（源文 `It was much later, after he had been made a prisoner…`）— `grep -n "要等许多之后" 译文/chapter-5.zh-CN.md`
9. **保留的法语原句内混入全角标点 3 处**（ch9：`*bien malade，hein*`、`*Trinquons，eh？*`、`*Sacré imbécile，va！*`；源文与 EN 块内均为半角 `,` `?` `!`）。术语表要求法语原句照录，全角化改动了原文形态。
   - 复现：`grep -n "Trinquons，eh？\|Sacré imbécile，va！\|bien malade，hein" 译文/chapter-9.zh-CN.md`
10. **附录类缺失：「挂起·待修」**——`原书/*.epub` spine 内的 `epigraph`（斯宾塞题诗 2 行 + 署名 `Spenser`）与 `dedication`（`To G. / Jean Aubry / in friendship / this tale of the last days of a / french brother of the coast`）在 `原文/` 与 `译文/` 中**均不存在**。
    - 定性（B-4）：属**文献附录类**（题词/献词），非叙事正文，**不判 D**；记 **B 级（应修）+ 挂起·待修**，不进「必须修」清单。
    - 修复点：重抽 `epigraph.xhtml`、`dedication.xhtml` 两页并补块对；`check_source_coverage.py` 的 `--min-chars` 阈值需下调（本次即因献词各段 <25 字符而漏报）。
    - 无正文内指代悬空（未触发 B-4 例外）。

## 五、C 级问题（建议优化）—— 共 5 类

1. **引号字形三体系并存**（跨章版式分裂）：ch2（`"`=120）、ch11（`"`=256）、ch13（`"`=50）全篇直引号；ch3 全篇直角引号 `「」`（53 对）；其余 13 篇弯引号 `“”`（ch5 另有 4 对 `「」` 作内层引号）。建议全书统一为 `“”/‘’`。
2. **中文块内 Markdown 星号斜体 127 处**（`*salle*`、`*citoyen*`、`*Salut*`、`*Parbleu!*`、`*chef d'escadre*` …），且与不带星号的首现括注体例混用（如 `塔尔塔纳（tartane，地中海单桅三角帆船）` 不加星号，`正厅（*salle*）` 加星号）。建议统一为「中文（原词）」不加星号，或全书统一加星号。
3. **EN 对照块 4 处双省略号**：ch10 `"The daughter!……"`、`"I see.……"` 等，源文为 `. …`。属标点形态问题，不影响中文读者。
4. **B-1 字形档归一化计数（全库性写盘副产品，按口径不参与定级，单列计数）**：`原文`→EN 块 `U+FEFF` 406→169、`U+200A` 124→61（逐章不一致：ch7/ch9/ch10 块内尚有残留，其余为 0）；`*emphasis*` 星号 source→block 删除 20 处 / block 新增 6 处（ch7 `con amore`、ch10 `Monsieur l'Abbé` 等）；弯引号转直、`—`→`--`。
5. **轻微脱字/生硬 1 处**：ch1 `在五千里格外、二十年之前`（源文 `five thousand leagues from here and twenty years ago`，疑脱「之」，宜作「五千里格之外」）。另 ch11 原文的 `---` 分隔线未进入块对，而 ch16 同形分隔线进入了块对——**分隔线处理口径全书不统一**（计数不影响语义）。

## 六、逐项清单结论

| # | 检查项 | 结论 | 缺陷数 |
|---|---|---|---|
| 1 | 语义错配 | 未发现（块对逐段核对，中文均与其紧邻英文块对应） | 0 |
| 2 | 整段漏译 | **命中 1 处（ch12，双侧同缺）** | 1（A） |
| 3 | 章末截断 | 未发现（16 章末段均已核；ch16 收束于「桑树……在它全部叶子的一个颤栗里轻轻地叹息了一声」） | 0 |
| 4 | 重大数字错译 | 未发现。抽验 60+ 处数字/年代：六个月、五十八年、四十五年、近五十年、六七万法郎、一千车/两千车、一百二十码、一百码、六千英里、八千里、六十里格、八英里、二十二炮、十二磅炮、十八磅炮、七十四炮舰、四十吨、五节、二十四小时、两千五百法郎、十一点钟等**全部正确**（注：全书仅 ch16 一处出现阿拉伯数字 `40`，译作「四十吨」） | 0 |
| 5 | 术语表违规 | **命中 2 类**：`presbytery`（5/6 未落位）、`ci-devant`（1/6 作「旧贵族」） | 2 类 / 6 处 |
| 6 | 同名异译 | **命中 2 类**：`Petite Passe/the Passe` 三形；ch1 同一军官「海军上校/上尉」 | 2 类 |
| 7 | 英文单词残留 | 0 违规（剔合法保留后 20 处全为专名/外语原句；中文侧无汉字外窜的英文句中词） | 0 |
| 8 | 语义误译 | 通读未见时态/主体/因果反转；ch8 命令原文经逐句核对准确（`a clear statement besides hints…` → 「一项明确的陈述，并辅以种种暗示」，无误） | 0 |
| 9 | 风格与流畅度 | 康拉德绵长句法大体保住：多层级复合句以「——」破折与「而其」「而这一份」等承接，未出现成片过度断句导致的语义断裂；宜改进者为 2 类版式/体例（见 C1、C2）与 1 处脱字（见 C5） | 3 类 |
| ① | 英文对照块逐字损坏 | 字体档 20+4+… 处（含 U+FEFF/U+200A 归一、星号增删、4 处 `……`）；**词级档 4 处**（ch9/ch12/ch15×2），另 +1 处段落次序颠倒（ch5） | 词级 4+1 处 |
| ② | 段数三方对齐 | 1324/1324/1327；差口全部定位（ch11 分隔线口径、ch12 双侧同缺、ch15 合法拆分与歌谣附译） | 见 A1 |
| ③ | 双侧同缺 | **命中 1 处**（ch12，353 词整段 EN+CN 同缺） | 1（A） |
| ④ | 夹带与幽灵内容 | 未发现。中文段数与英文块在 15/16 章完全相等，唯一的 +5 为 ch15 法文歌谣逐行附中译（合法）；未见章末重复对话、杜撰署名块、幽灵残句；EN 块无 CJK 码点窜入 | 0 |
| ⑤ | 全库型 IME 错字 | **命中 1 处**：ch16「干真万确」（应为「千真万确」）；未命中「月人」同族及 `未/末`、`己/已`、`侯/候`、`辨/辩`、`检/捡` 等形近误用 | 1 |
| ⑥ | 原文素材完整性 | `--min-chars 25` 报缺失 2 段（epigraph 2 行诗）；独立复核另发现 `dedication.xhtml` 整页未抽取（预检阈值漏报）；逐章 `原文`↔epub 段级比对 16/16 一致，**无截断、无选集差异** | 1 项（附录类，B＋挂起） |
| ⑦ | 逐章译率核对 | 16 章译率 0.34–0.38，中位 0.38，**无章低于 0.20**；最低 chapter-12（0.34，其偏低与 A1 少一段相印证）、chapter-13（0.35）。部标题页不适用（本书无 `## Part` 页） | 0 |

## 七、等级判定理由

- **不是 A 优秀**：存在 1 处 A 级缺陷（ch12 整段双侧同缺）——A 档要求「无 A 级问题」，此条直接排除。
- **不是 C 需关注**：A 级只有 1 处，且非成片；B 级虽列出 10 条，但其中 7 条为**单点/单类**（次序颠倒 1 处、术语 2 类共 6 处、专名 3 形、称谓 1 处、引号 1 处、错字 2 处、法语标点 3 处），未构成「跨章异译十几组」「系统性器物误译」式的成片蔓延；语言质量本身（语义、数字、风格、术语主体、英文残留）实测全过。
- **判 B 良好**：与判据「无 A 级问题，但 B 级成片或含系统性错误；**或有 1–2 处 A 级而整体质量仍过硬**」的第二分句吻合——本书恰为 1 处 A 级 + 若干点状 B 级，整体质量过硬。
- **不判 D**：B-5 四条判据（≥10% 段落 / 整幕整节整篇 / ≥2 处章末终场缺失 / 短篇集某篇结尾整节缺失）**均不成立**；题词与献词属 B-4 的文献附录类，一律不判 D。
- 边界说明：若派发方收口口径把「EN 对照块 4 处词级差异 + 1 处段落次序颠倒」计为「成片」，则本项可落 **B/C 边界**，理由见上（4 处均为语义中性且分散在 4 个不同章、4 种不同类型）；本报告取 **B 良好**。

## 八、元数据备注（不改文件，仅提示人工）

1. **简报项目名与实际目录名不符**：简报与本次任务参数写作 `joseph-conrad_the_rover`（下划线），磁盘上实际为 `joseph-conrad_the-rover`（连字符）。用下划线名跑任何脚本会得到「<无原文>」。建议修档。
2. **阅读序陷阱**：`原文/` 与 `译文/` 采用 `chapter-N` 命名，字典序会把 `chapter-10` 排到 `chapter-2` 前；所有按序遍历均须按数字键排序（本次已按数字序核对）。
3. **预检覆盖面漏报**：`check_source_coverage.py --min-chars 25` 报「缺失 2 段」，实为 `epigraph.xhtml` 两行题诗；`dedication.xhtml`（献词整页，5 段）因每段短于阈值被静默过滤。若按「附录缺失」记账，实际是 **2 个前置项（epigraph + dedication）**，不是 2 个段落。
4. **预检口径误报（逐条实测）**：
   - `Allons—du courage` 标 `★源0次(幽灵?)` → **非幽灵**，源文 ch11 实有 `*Allons—du courage*`，"Allons" 与 "—" 之间夹 U+FEFF/U+200A 致精确匹配失败。
   - `lateen 源×37 译×8 落位偏低` → **误报**：源文全书中 `lateen` 仅 **4 次**，4 处全部译作「三角帆」（ch15×2、ch16×2）。
   - `the Terror 源×7 译×1 落位偏低` → **误报**：源文 `the Terror` 仅 **1 次**（ch11，译「恐怖统治」），预检显然把 `terrorist/terrors` 一并计入了。
   - `la terre 源×2 译×20` → 源文作 `*La terre! La terre!*`（首字母大写），大小写敏感匹配导致计数失真，译文处理正确。
   - 「残留」列（`漫游者×67`、`公民塞沃拉×20`、`阿米莉亚号×59`、`苏珀布号×2`）为译文自身用词，**非违规**，属预检列义误解。
5. **项目说明体例与实际略有出入**：`项目说明.md` 称「章节用罗马数字（I—XVI）编号，**照搬不译**」，实际译文标题为 `## I / 第一章`（保留罗马数字并附中文序数）。属体例选择，非缺陷，仅提示人工确认是否与既定体例一致。
6. **未观察到「术语表幽灵/变体条目反向污染英文对照块」形态**：EN 块中无依据术语表变体条目改写原文的痕迹（唯一系统性改动是 markdown 强调星号的增删，见 C4）。
7. **无产物级缺陷**：无 `_partN` 分片；`译文/` 16 个文件 mtime 为 2026-08-12 20:19 ~ 08-13 00:12，晚于 `原文/`（08-12 18:07–18:08），主交付物完整，不属于「未完成产物」。

## 九、复核方法与局限

- 已读：`复核_20260814批/简报模板.md`、`预检/joseph-conrad_the_rover.txt`、`项目说明.md`、`术语表.md`、`原文/*.md` 全文（16 篇，用于逐字比对与素材核对）、`译文/*.zh-CN.md` 全文（16 篇，`===Chinese===` 侧逐段精读，`===Original===` 侧用于比对与定点核验）、`原书/*.epub` 解包后的 `spine` 与 `text/*.xhtml`。
- 未读（按盲审纪律）：项目内既有 `审核报告.md`、`Plan.md`、`翻译项目/` 及根目录下的 `阶段三审校批次记录_*.md`、`审校统计_*.md`、`原文素材完整性扫描_*.md` 中与本书相关的旧结论。
- 程序化比对方法：对每章取「`原文/<ch>.md`（去章题行）」「译文 EN 块」「译文 CN 块」三方做字符归一化（U+FEFF/U+00A0/U+200A/U+200B 剔除、弯引号转直、破折号统一、空白压缩）后的 `difflib` 级联比对，逐条输出替换/插入/删除；再对 epub `chapter-N.xhtml` 做 `<p>` 段级抽取与 `原文/*.md` 段级比对，用于区分「素材缺失」与「块/译文缺失」。
- 抽样说明：数字/年代为**系统抽验**（列出全部含数词+量词的块，约 60 处逐一核对中文数字），非仅随机抽样；语义与风格为**全文精读**（CN 侧 16 篇通读），英文侧以程序化 diff + 定点回查为主。
- 存疑/未决（建议人工确认）：
  1. ch5 段落次序颠倒是否为块准备环节的有意重排（源文与 epub 次序一致，译文两处同步错位，倾向判定为缺陷，但无法从文件本身确证成因）。
  2. 「`Petite Passe` 译名三形」是否应统一为哪一形（术语表未收此条，本书建议「小海峡」与首现括注一并固定）。
  3. ch1 「海军上校 / 上尉」应统一为哪一形（源文 `captain` 为海军上校衔，但与术语表 `Captain Réal → 上尉` 存在潜在冲突，建议人工裁定后回填术语表）。
  4. epigraph/dedication 是否需补译（附录类，按 B-4 不阻塞发布）。
