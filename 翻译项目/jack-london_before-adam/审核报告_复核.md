# 《亚当之前》（Before Adam）独立复核报告（2026-09-21 批次盲审复核）

审校日期: 2026-09-21
质量等级：B 良好
一句话结论：素材与块段完全对齐、英文对照块词级零损坏，唯术语漂移跨 6 章（2 类）加附录类素材未抽，落 B。

## 一、项目与核对范围

- 项目：`jack-london_before_adam`（杰克·伦敦 1907 年进化幻想长篇《Before Adam》）
- 章节数 / 块数 / 体量：
  - `原文/` 20 篇 `.md`（210,845 字节）；`译文/` 20 篇 `<同名>.zh-CN.md`（392,421 字节），无 `.all.zh-CN.md` 合并产物干扰。
  - **块数 O=C=142**；段落 **原文 425 / 译文EN块 425 / 译文CN块 425**，三方零不对称。
  - 英文对照块合计 209,611 字符，中文侧 58,950 汉字。
  - 篇目：`epigraph`（题词）+ `i`–`xviii`（正文 18 章，罗马数字编号）+ `list-of-illustrations`（插图目录）。
- **阅读顺序及其核定依据**：以 `python scripts/epub_spine.py jack-london_before-adam` 为准，spine 内容序为
  `epigraph → frontispiece → chapter-1 … chapter-18 → loi`（21 项内容页，另有 5 项非内容项）。
  映射到落盘文件名即 `epigraph → i → ii → … → xviii → list-of-illustrations`。
  **文件名序陷阱确实存在**：`原文/`、`译文/` 的罗马数字小写文件名按字母序会把 `ix` 排在 `iv` 之后、`v` 之前（`ls` 实际顺序：`… iv, ix, list-of-illustrations, v, vi …`），**字母序 ≠ 阅读序**，本报告一律按 spine 序核对（见第八节）。
- 已跑工具与命令（全部只读）：
  - `python scripts/epub_spine.py jack-london_before-adam`
  - `python scripts/check_source_coverage.py jack-london_before-adam --min-chars 25` → `检查 424 段 (阈值25), 缺失 0 段`
  - `python scripts/check_chapter_ratio.py jack-london_before-adam --min-ratio 0.20` → `20章 译率中位=0.32`，最低 `list-of-illustrations` 0.25，无一章跌破 0.20
  - 自建只读脚本（`%TEMP%\xad_check\`，未落在项目目录）：块段三方表、EN 块逐段逐词 diff、英文残留扫描、术语落位回读、epub 元素级对回、字形/异常字符审计、实体锚点对齐。

## 二、数据层核对（程序化结论）

| 项 | 结果 |
|---|---|
| 块数 O=C | **OK**：20 篇逐篇相等，合计 O=C=142（`analyze.py` 逐文件表） |
| 段数 原文/EN/CN 三方 | **OK**：425 / 425 / 425，逐块 EN 段数 == CN 段数（0 块不对称），逐篇 `srcP-EN=0`、`srcP-CN=0` |
| 英文对照块逐字 diff | **词级 0 处损坏**；425 段中 370 段逐字全同，55 段为**纯字形档**（EN 块比 `原文/` 少 72 个 U+FEFF、4 个 U+200A——不可见字符，原文侧抽取残留）。**无换词/脱字/破词/中文串入/整句覆盖** |
| 英文残留（剔合法保留） | **0**：CN 块剔除全部括注后，拉丁串出现 **0 次**（distinct=0 / total=0）；全角括注共 34 处，均为首现译名标注 |
| 术语表硬约束落位 | **2 类违规**（详见第四节 B-1/B-2/B-3）；其余 33 条落位正确。预检报的「catamaran 落位偏低 4/7」「slough 残留学航海」「stringy-rooted carrots 残留」经回读均为**子串/口径假阳性**（`xiii` 第 2、3 处以「筏子」承前指代，合法） |
| 幽灵条目/归一残留 | 术语表「候选专有名词」表含 `Also/Thus/Nor/Sometimes/Possibly/Many/Remember` 等**建库期误植**条目（非硬约束行），不计违规；硬约束 35 行无幽灵条目 |
| 原文素材覆盖（含 `--min-chars 25`） | **0 段缺失**（424 段全数命中）。另做 **epub 元素级对回**：18 章 + epigraph + loi 的全部 `<h2>/<p>` 元素文本逐条命中 `原文/`（3 处初报「未命中」经复核为 `<i>`→`*斜体*` 归一差异，非缺失） |
| 逐章译率（`check_chapter_ratio`） | **全部达标**：中位 0.32，区间 0.25–0.32，**无一章跌破 0.20**，无「整章未译」形态 |
| 异常字符 / 引号字形 | CN 块 **U+FEFF/U+00A0/U+200A/U+200B/U+FFFD/U+3000 全部为 0**；标点全角规范（`，`2868 `。`2410 `、`269 `；`141 `：`81 `！`39 `？`23），CN 块内半角句点 **0**；**425 个 CN 段全部以终止标点收尾**（非终止收尾 0 段），无章末截断 |

## 三、A 级问题（必须修）—— 共 0 处

九项 A 行（语义错配 / 整段漏译 / 章末截断 / 重大数字错译）**逐块逐段通读 + 程序化双重复核，未发现任何 A 级缺陷**。支撑证据：

1. **无整段漏译 / 无双侧同缺**：142 块逐块 EN 段数 == CN 段数；`原文/` 段落 425 == EN 块段落 425 == CN 块段落 425，且 EN 块与 `原文/` **词级逐字一致**（专项① 0 处损坏）。三向闭合意味着不存在「块内 EN↔源文段落索引跨度 > 段数」的吞段空间。
2. **无段数对称下的整段置换**：以 20 个人名/族群名做**实体锚点对齐**（`anchor.py`），519 处命中中 519 处均能在同一段中文找到对应译名（6 处「未命中」经回读为「族人/族里」「族群/族人」「火族/火人」同义变体，非错配）。
3. **无章末截断**：20 篇各自末段中英对读，语义均完整收束（如 `xviii` 末段 `He is Red-Eye, the atavism.` →「他就是红眼，那个返祖者。」；`xiii` 末段 `…though first we had to evict a couple of pugnacious youngsters…` →「…虽然得先赶走两个占着洞的好斗的小家伙。」）。
4. **无重大数字错译**：425 段逐段**数字序列比对**（`\b\d[\d,\.]*\b`）**0 处不一致**；文中关键数字逐处回读无误——`a hundred and thirty pounds`→一百三十磅、`one hundred and seventy pounds`→一百七十磅、`twenty feet`→二十英尺、`seventy feet`→七十英尺、`thirty or forty sounds`→三四十个声响、`three miles`→三英里、`a hundred yards`→一百码、`thirty of us`→三十个人、`eight of the Folk`→八个、`a score of the hunters`→二十个猎手、`sixth winter`→第六个冬天、`three years / four years`→三年 / 四年、`a thousand centuries`→一千个世纪。

## 四、B 级问题（应修）—— 共 4 类

### B-1 术语表硬约束违规：`wizened old hunter` 一名四译（跨 4 章，4 处）
术语表定名「干瘪瘦小的老猎人」，备注明写「火族瘸腿老猎人，**各处统一**」，实际出现 4 种形态：

| 位置 | 中文实际写法 | 复现命令 |
|---|---|---|
| `译文/vii.zh-CN.md:36` | 「他还老了，**老得干枯**」（首现，未按定名） | `grep -n "老得干枯" 译文/vii.zh-CN.md` |
| `译文/xiii.zh-CN.md:53` | 「那个**干瘪的**老猎人」 | `grep -n "干瘪的老猎人" 译文/xiii.zh-CN.md` |
| `译文/xv.zh-CN.md:67` | 「那个**瘸腿的干瘪**老猎人」 | `grep -n "瘸腿的干瘪老猎人" 译文/xv.zh-CN.md` |
| `xiii:13 / xv:103 / xvii:29 / xviii:41` | 「那个**干瘪瘦小的**老猎人」（合规 4 处） | `grep -n "干瘪瘦小的老猎人" 译文/*.md` |

英文侧对应：`vii` `He was old, too, wizened with age`；`xiii` `the wizened little old hunter`；`xv` `the wizened old hunter who limped`；`xvii` `the little wizened old hunter`；`xviii` `the little wizened old hunter`——同一人物，5 次出现 4 种译法。**判 B**（术语表违规，非语义错，情节不受影响）。

### B-2 术语表硬约束违规：`atavism / atavistic` 抽象义另起「隔世遗传」（跨 3 章，4 处）
术语表定「the atavism → 返祖者」，备注「红眼的称号；**泛指返祖现象作「返祖」**」，未收「隔世遗传」。实际正文两套并行：

- **「隔世遗传」**（4 处）：`译文/i.zh-CN.md:21`「红眼的淫欲与**隔世遗传**」（源 `the lust and the atavism of Red-Eye`）、`译文/ii.zh-CN.md:99`「更具**隔世遗传**性」「一场**隔世遗传**的梦魇」（源 `more atavistic` / `an atavistic nightmare`）、`译文/ii.zh-CN.md:103`「这个**隔世遗传**的大脑」（源 `this atavistic brain`）、`译文/ix.zh-CN.md:13`「红眼是个**隔世遗传**的返祖现象」。
- **「返祖 / 返祖者 / 返祖现象」**（6 处）：`译文/v.zh-CN.md:55,57`、`译文/ix.zh-CN.md:13`、`译文/xiv.zh-CN.md:25,39`、`译文/xviii.zh-CN.md:71`。

复现：`grep -n "隔世遗传" 译文/*.md` / `grep -n "返祖" 译文/*.md`。**判 B**（同一源词两套译名并行，属术语漂移，非注释/义项切分豁免范围）。

### B-3 术语表内部自相矛盾导致 `stringy-rooted carrots` 一名两译（2 处）
术语表定名「粗筋胡萝卜」，备注却写「与前文『**粗筋多渣的胡萝卜**』统一」——表内自相矛盾，译文随之分裂：
- `译文/v.zh-CN.md:113`「我们吃了一顿**粗筋多渣的胡萝卜**」（源 `we made a meal of stringy-rooted carrots`）
- `译文/vi.zh-CN.md:83`、`译文/xv.zh-CN.md:53`「**粗筋胡萝卜**」（合规 2 处）

复现：`grep -n "粗筋" 译文/*.md`。**判 B**（术语变体，与 B-1/B-2 同族，可并计为「术语漂移」一大类）。

### B-4 素材完整性（专项⑥ / R5）：epub front·back matter 未抽未译（文献附录类）
按 `epub_spine.py` 的 21 项内容页与 `原文/` 20 篇逐一对照，**唯一未落盘的内容页为 `frontispiece.xhtml`（纯图，正文 0 字符）**；另有一批非内容页未抽：

| epub 页 | 可见字符 | 性质 | 是否落盘 |
|---|---|---|---|
| `frontispiece.xhtml` | 0（仅 `<img alt>` 地图说明） | 卷首插图 | **无损失**：其 `alt` 文本与 `loi.xhtml` 完全同文，而 `loi` 已抽取并翻译 |
| `halftitlepage.xhtml` | 11（"Before Adam"） | 书名半扉页 | 未抽 |
| `titlepage.xhtml` | 28（"Before Adam By Jack London"） | 书名页 | 未抽 |
| `imprint.xhtml` | 1,195 | Standard Ebooks 版权/授权样板 | 未抽 |
| `uncopyright.xhtml` | 2,060 | Standard Ebooks Uncopyright 样板（含 `<blockquote>` 题诗） | 未抽 |
| `colophon.xhtml` | 1,027 | Standard Ebooks 版本信息 | 未抽 |
| `toc.xhtml` | 804（导航） | 目录 | 未抽 |

**判 B（应修，不进「必须修」清单）**：按 B-4 分档，版权页/书名页/目录/出版方说明属**文献附录类材料**；未造成正文内指代悬空（正文从不引用书名页或版权页），且均为 **Standard Ebooks 出版方样板文字、非作者正文**。
**两可读法并列供派发方裁量**：(读法一) 按 B-4 表列，记 B 级（应修），建议补抽 `imprint/uncopyright/colophon`；(读法二) 视为出版方元数据、不属作品本体，记「非缺陷」。**两种读法均不构成 D**（不满足 B-5 四条判据任一）。

## 五、C 级问题（建议优化）—— 共 8 类

| # | 类型 | 位置与证据 | 复现命令 |
|---|---|---|---|
| C-1 | IME 别字 | `译文/vi.zh-CN.md:41`「从来没有**哪么**高明的生灵被这么彻底地挫败过」——「哪么」应为「那么」 | `grep -n "哪么" 译文/*.md` |
| C-2 | IME 别字 | `译文/x.zh-CN.md:41`「像米诺鱼**似地**窜来窜去」——「似地」应为「似的」 | `grep -n "似地" 译文/*.md` |
| C-3 | 成语误写 | `译文/viii.zh-CN.md:119`「它们**三下两除二**就结果了她」——成语应为「三下五除二」 | `grep -n "三下两除二" 译文/*.md` |
| C-4 | 排版残字 | `译文/ii.zh-CN.md:35`「这就是为什么**你我 在**梦里从不撞到地面。」——「你我」与「在」之间混入半角空格 | `grep -n "你我 在" 译文/ii.zh-CN.md` |
| C-5 | 译文重复破句 | `译文/iv.zh-CN.md:69`「它比**我们所知的人类——我们所知的人类**——最笨拙的手艺还要笨拙无数倍。」源 `than the clumsiest handiwork of man—of man as we know him`；中文把 `of man` 与 `of man as we know him` 同译为「我们所知的人类」，读成重复语病 | `grep -n "我们所知的人类——我们所知的人类" 译文/iv.zh-CN.md` |
| C-6 | 具体细节替换 | `译文/x.zh-CN.md:95`「**隔着一千年**、透过梦的媒介回望她」源 `looking back upon her **from the twentieth century**`；「二十世纪」被换成「一千年」（同书 `iv:41` 对 `the twentieth-century boy` 是照译「二十世纪的男孩」，口径不一） | `grep -n "隔着一千年" 译文/x.zh-CN.md` |
| C-7 | 数字近似表述 | `译文/xiv.zh-CN.md:69`「有时**六七个**节奏同时开敲」源 `Sometimes **half a dozen** rhythms`；`译文/v.zh-CN.md:69`「向后足足飞出**十几英尺**」源 `fully **a dozen** feet`。均为「半打/一打」的模糊化，非硬性数字错 | `grep -n "六七个节奏" 译文/xiv.zh-CN.md`；`grep -n "十几英尺" 译文/v.zh-CN.md` |
| C-8 | 首现括注次序 | 术语表要求 `Marrow-Bone`、`Hairless One`「首次出现标注」，但首现处（`viii:15`「老啃骨髓」、`viii:47`「无毛人」）无括注，括注推迟到 `ix:85`「老啃骨髓（Marrow-Bone）」、`ix:95`「无毛人（Hairless One）」 | `grep -n "老啃骨髓（Marrow-Bone）" 译文/ix.zh-CN.md` |
| C-9 | 语序生硬 | `译文/v.zh-CN.md:53`「他毛多得可憎。**毛不很重在我们那里是体面事。**」源 `It was a matter of pride with us to be not very hairy.`——状语中插，读来打结 | `grep -n "毛不很重在我们那里" 译文/v.zh-CN.md` |
| C-10 | 动物指称不一致 | 同一类野兽在 `xiii:39` 用「**他们**低吼了一声」（狮子），在 `vi:41` 等处用「**它**」；全书 它/他 指兽不统一 | `grep -n "然后他们低吼了一声" 译文/xiii.zh-CN.md` |

另：**字形档单列计数（B-1 口径，C 级、不据此下调总等级）**——EN 对照块相对 `原文/` 丢失不可见字符 **U+FEFF ×72、U+200A ×4**（原文侧为抽取残留，EN 块已归一）；CN 侧无任何异常字符。

## 六、逐项清单结论

| # | 检查项 | 结论 | 缺陷数 |
|---|---|---|---|
| 1 | 语义错配 | 无（实体锚点 519/519 对齐，6 处为同义变体） | 0 |
| 2 | 整段漏译 | 无（425/425/425 三方闭合） | 0 |
| 3 | 章末截断 | 无（20 篇末段中英对读完整，425 段全部终止标点收尾） | 0 |
| 4 | 重大数字错译 | 无（段落级数字序列 0 不一致；关键数字逐处回读无误） | 0 |
| 5 | 术语表违规 | **有**：wizened old hunter 一名四译；atavism 抽象义「隔世遗传」并行；粗筋胡萝卜两译 | 3 类 |
| 6 | 同名异译 | 有（与第 5 项同源，均属同一人物/同一源词的译名分裂） | 并计于上 |
| 7 | 英文单词残留 | 无（剔括注后 0 次） | 0 |
| 8 | 语义误译 | 无（无时态/主体/因果反转）；仅 C-6 细节替换 1 处 | 0（C 级 1） |
| 9 | 风格与流畅度 | 轻微（C-5 重复破句、C-9 语序、C-10 指称；整体文学性良好，无成片机翻腔） | C 级 3 |
| 专项① | 英文对照块逐字损坏 | **词级 0 处**；纯字形档 55 段（U+FEFF/U+200A 归一） | 0（C 级字形档 1） |
| 专项② | 段数三方对齐 | 零不对称（425/425/425，逐块 EN==CN 段数） | 0 |
| 专项③ | 双侧同缺 | 无（EN 块与源文词级逐字一致 + 段落三向闭合 + epub 元素级全命中） | 0 |
| 专项④ | 夹带与幽灵内容 | 无（CN/EN 长度比无 >0.60 离群段；无杜撰署名块、无幽灵残句） | 0 |
| 专项⑤ | IME 错字扫描 | 有零星 3 处（哪么/似地/三下两除二）+ 1 处空格残字，语义均可复原 | 4 |
| 专项⑥ | 原文素材完整性 R1–R5 | R1 无（无多 `<article>`/多篇目页，逐章 1 个 `<h2>`）；R2 无（`<blockquote>` 仅 2 处：`epigraph` 已抽、`uncopyright` 属附录类）；R3 无（`alt` 仅地图说明与 SE logo，地图说明已在 `loi` 捕获）；R4 无（全书 `<tr>`=0，非剧本）；**R5 有**（6 个附录类页未抽，见 B-4） | B 级 1 类 |
| 专项⑦ | 逐章译率 | 全部达标（中位 0.32，最低 0.25，无章跌破 0.20） | 0 |

## 七、等级判定理由

**判 `B 良好`。**

1. **A 级为 0 处**，九项 A 行逐块逐段通读并程序化复核（三方段落闭合 + 英文块词级逐字 + 数字序列 + 实体锚点 + 章末对读）均无缺陷。
2. **但数据层「术语零违规」一项未清**，故不得评 A：`wizened old hunter` 一名四译（跨 vii/xiii/xv/xvii/xviii）、`atavism` 抽象义「隔世遗传」与「返祖」两套并行（跨 i/ii/ix 与 v/ix/xiv/xviii）、`stringy-rooted carrots` 两译。术语漂移**跨 6 章成片**，已超出「单一术语漂移 ≤1 类」的豁免范围（该豁免仅适用注释/义项切分，正文落位不适用）。
3. **中文侧无「语义级字词损伤」**：3 处别字/成语误（哪么、似地、三下两除二）语义均可复原，按本批口径记 C，不阻塞等级；无换词误译、无缺谓语破句。
4. 另有 1 类附录类素材未抽（B-4），属 B 级（应修），不进「必须修」清单。
5. 对照判据：「`B 良好`：无 A 级问题，但 B 级成片或含系统性错误」——本稿 B 级 4 类、其中术语漂移系统性跨章，**正落 B**。
6. **两可情形并列**：若派发方认定 B-4（Standard Ebooks 出版方样板页未抽）属「非缺陷」、且把术语漂移按「同一源词变体」压缩为 1 类，则 B 级降为 2 类、接近 A 的边缘；本报告仍按「术语表硬约束行明写『各处统一』而实际分裂」判 B。**无论哪种读法都不满足 D 的 B-5 四条判据**（无 ≥10% 段落缺失、无整幕/整节/整篇缺失、无 ≥2 处章末缺失、非短篇集单篇结尾缺失）。

## 八、元数据备注（不改文件，仅提示人工）

1. **阅读序陷阱（须修档）**：`原文/`、`译文/` 用罗马数字小写文件名，**字母序 ≠ 阅读序**——`ls` 实际顺序把 `ix` 排在 `iv` 之后、`v` 之前（`… iv, ix, list-of-illustrations, v, vi, vii, viii, x …`）。凡按文件名排序处理者必错，请以 `epub_spine.py` 为准。
2. **术语表自相矛盾（须修档）**：`术语表.md:49` `stringy-rooted carrots → 粗筋胡萝卜`，备注却写「与前文『粗筋多渣的胡萝卜』统一」——定名与备注互相冲突，是 B-3 的直接成因。
3. **术语表「候选专有名词」表含建库期误植**：`Also / Thus / Nor / Sometimes / Possibly / Many / Remember` 被当作专有名词列出（`术语表.md:61-76`），非源文专名，按误报防线第 4 条不作硬约束。
4. **术语表备注措辞待校**：`slough → 汊口` 备注称「地名」，实际源文中 `slough` 是汇入主河的**河汊/泥水汊**（通名），非专有地名；`Fire-man / Fire-men → 火人` 备注「前文章节已用」在本单本项目中指代不明。
5. **预检假阳性备案**：预检报 `catamaran 落位偏低 4/7`、`slough 残留「学航海」×1`、`stringy-rooted carrots 残留「粗筋多渣的胡萝卜」×1`、`the atavism 落位偏低 1/3`——经逐处回读，前两条为**子串/承前指代假阳性**（`xiii` 后两处用「筏子」承前），后两条**属实**（已分别计入 B-3、B-2）。预检报「英文残留 top: 无」与本次独立扫描一致。
6. **`项目说明.md` 前提核对**：称「全书 20 篇、约 206KB」——与实际 20 篇、210,845 字节相符；「块对照，每 3-6 段一组」与实际 142 块 / 425 段（均值 3.0 段）相符。**未发现前提错误**。

## 九、复核方法与局限

**读了哪些文件**：`原文/` 全 20 篇、`译文/` 全 20 篇（双语对照块，含英文块与中文块）逐段通读一遍；`术语表.md`、`项目说明.md`；`原书/jack-london_before-adam.epub` 解包后的全部 25 个 `.xhtml`（含 `content.opf`、`toc.ncx` 未在正文引用者亦查验）。**未读取**本项目旧 `审核报告.md`、`Plan.md`、`translation_queue.csv`、根目录 `Plan.md` 及任何批次记录/统计文件，未读取其他项目复核报告（盲审红线遵守）。

**用什么工具**：`epub_spine.py`（阅读序）、`check_source_coverage.py --min-chars 25`（素材覆盖）、`check_chapter_ratio.py --min-ratio 0.20`（逐章译率）；另在 `%TEMP%\xad_check\` 自建 8 个只读脚本做：块段三方表、EN 块逐段逐词 `difflib` diff（字形档/词级档分计）、CN 块括号外拉丁串扫描、术语逐条落位回读（打印源文命中段与对应中文段）、epub 元素级对回（`<h2>/<p>/<li>/<blockquote>/<header>` + `alt`）、字形与异常字符审计、段落级数字序列比对、实体锚点对齐、CN 段末标点完整性。全程未写出项目目录以外任何文件到项目内；唯一写出文件为本报告。

**哪些地方是抽样**：
- 术语落位为**逐条全查**（35 条硬约束行全覆盖），非抽样；`Folk/horde` 等高频词以「逐命中段落回读 + 实体锚点」双重核对。
- epub 元素级对回为**全量**（每页每个段落级元素），非抽样。
- 语义对齐为**全量通读**（425 段逐段中英对读），未采用抽样，故不存在未读段落。

**存疑 / 未能定论**：
1. B-4（6 个附录类页未抽）的定级存在「B 级（应修）」与「非缺陷」两读，已在第七节并列，请派发方裁量。
2. `uncopyright.xhtml` 内确有一处 `<blockquote>` 承载题诗（`May you do good and not evil…`），属 R2 同族形态，但内容为 Standard Ebooks 授权样板而非作者正文，故未按 R2 记 A；若派发方口径要求连出版方样板一并补抽，应将其并入 B-4 修复清单。
3. C-6（「二十世纪」→「隔着一千年」）的严重度两可：可读作译者的无害自由（叙述者同属「现代」），也可读作具体细节替换；本报告按后者记 C，若从严可升 B。
4. 本报告未逐字校对 `list-of-illustrations` 的地图描述与 epub `loi` 之外的图像内容（epub 仅 1 幅正文插图，已由 `loi` 文本覆盖），图像本体不在纯文本比对范围。
