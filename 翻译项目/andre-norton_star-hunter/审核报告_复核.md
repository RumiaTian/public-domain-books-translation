# 《星猎》独立复核报告（2026-08-25 批次复核）

审校日期: 2026-09-21
质量等级：B 良好
一句话结论：全书 935 段英文对照块与原文逐字一致、素材零缺失、无任何 A 级缺陷，仅术语层有 3 类非致命偏差，属高完成度交付物。

## 一、项目与核对范围

- 项目：`andre-norton_star-hunter`；体裁：Andre Norton 科幻中篇《Star Hunter》（1961），单一叙事弧。
- 章节数：13（`epub_spine.py` 报 spine content 13 / total spine 17）。
- 块数：`===Original===`/`===Chinese===` 对 **268 对**（O=268, C=268，零余块、零缺块；无多余 Chinese 分隔符）。
- 体量：`原文/` 13 文件共 190,556 字节、**935 个叙事段** 另加 3 条 `---` 分隔线；`译文/` 13 文件共 374,033 字节，中文块计 **49,568 汉字**。
- 阅读顺序及其核定依据：以 `python scripts/epub_spine.py andre-norton_star-hunter` 的 **spine 序**核定 —— `chapter-1 … chapter-13` 与 `原文/01-chapter-1.md … 13-chapter-13.md` 的文件名序**完全一致**（两位数字零填充），**不存在** `chapter-10` 排在 `chapter-2` 前之类的字典序陷阱；`译文/` 无 `*.all.zh-CN.md` 合并产物、无 `_partN` 分片，故统计不含任何非章节件。
- 已跑工具与命令（均只读；对照脚本在 `%TEMP%\starhunter_rev\` 自建）：
  - `python scripts/check_bilingual.py <译文md>`（13 章逐一，结构契约全部「满足」）
  - `python scripts/check_coverage.py <原文md> <译文md>`（抽样 01/05/13 章）
  - `python scripts/check_source_coverage.py andre-norton_star-hunter --min-chars 25`
  - `python scripts/check_chapter_ratio.py andre-norton_star-hunter --min-ratio 0.20`
  - `python scripts/epub_spine.py andre-norton_star-hunter`
  - 自建只读脚本（置于 `%TEMP%\starhunter_rev\`）：`epubcmp.py`（epub 元素级对回）、`diff3.py`/`raw.py`/`feff.py`（逐字 diff 与 FEFF 归因）、`termdrift.py`/`gloss.py`/`civ.py`/`safari.py`（术语落位）、`residue2.py`（英文残留）、`nums.py`（数字交叉核）、`polarity.py`（否定极性双向核）、`outlier.py`（长度比离群）、`ime.py`（稀有汉字扫描）、`dup.py`（重复段检测）

## 二、数据层核对（程序化结论）

| 项 | 结果 |
|---|---|
| 块数 O=C | **268 = 268**，13 章逐章相等（19/19/18/16/14/21/19/24/22/20/23/29/24），无缺块、无重叠块 |
| 段数 原文/EN/CN 三方 | 原文 **935** 叙事段 + 3 `---`；译文 EN 块 **935**；译文 CN 块 **935** + 3 `---` → **三方零不对称**。预检报的 3 处 `+1`（03/09/13 章）经逐块回读，**系 epub `<hr>` 转为 `---` 后只落在 CN 侧、EN 侧未复制该行所致**，为计数口径，非漏译 |
| 英文对照块逐字 diff | **词级档 0 处**（换词/脱字/破词/中文串入/整句覆盖全部为 0）；**字形档 79 段**，全部为 U+FEFF 剥离（原文含 270 个 U+FEFF 行内连字符前缀，译文 EN 块保留 176 个，消失 94 个）。**无「steamer→ste轮」类破词、无中文窜入英文块** |
| 英文残留（剔合法保留） | 剔除首现括注后仅 **X-Tee×7、L-B×1**；二者均为 `术语表.md` 明定「缩写照搬不译」。**实际违规 0 处** |
| 术语表硬约束落位 | 80 条逐条复算。**违规 1 处**（`Veep→帮主`，08 章）；**定名缩略 6 处**（`civ→客户`，04/08 章）；其余 78 条 100% 落位。预检报的 10 项「未落位/落位偏低/幽灵条目」**逐条回读后全部证伪**（详见 §八） |
| 幽灵条目/归一残留 | 无幽灵条目。预检标注 `psycho-tech 源×0` 系其正则未匹配复数 `psycho-techs`（原文 01 章第 64 行实有 1 次，译 1 次）；`the Center 1/1`、`Master 1/1`（原文只有 1 处 `Masters`，非 2 处）均 100% 落位 |
| 原文素材覆盖（含 --min-chars 25） | `check_source_coverage.py`：**检查 843 段，缺失 0 段**。另做 epub **元素级对回**（`epub/text/chapter-*.xhtml` 的 `<p>`/`<h2>`/`<hr>`/`<blockquote>`/`<header>`/`<img>` 计数与文本逐段比）：13 章 `<p>` 合计 **935 = 原文 935**、`<h2>` 13 = 13（I–XIII）、`<hr>` 3 = 3；章内 `<blockquote>`/`<header>`/`<img alt>` 均为 **0**，故**不存在**多 `<article>` 只抽首篇、`<img alt>` 公式漏抽、`<blockquote>`/`<header>` 块漏抽、名单/题注/信件抬头漏抽等已知失效模式。仅 epub 前/后附 4 个 **非叙事** 页未抽（titlepage/imprint/colophon/uncopyright，共 14 段），属 Standard Ebooks 版权与版本说明类，按 B-4 记 B 级（见 §四） |
| 逐章译率（check_chapter_ratio） | 13 章 **中位 0.344，最小 0.316（09 章）／最大 0.368（01 章）**，全部落在正常带 0.30–0.45 内，**无章跌破 0.20**、无 `the-black-gang ch14 = 0.03` 型整章未译 |
| 异常字符 / 引号字形 | 弯引号 `“”‘’` 与原文/EN 块**字形一致，无转直**；U+00A0/U+200A/U+2009 无异常归一。**2 处未闭合 `「`**（12 章第 11 行、13 章第 277 行），见 §五 |

## 三、A 级问题（必须修）—— 共 0 处

对 268 个块对、935 个段对做了**逐段语义对齐精读**（非抽样），结论：**未发现任何 A 级缺陷**。逐项排除依据：

1. **语义错配（A）**：0 处。935 个段对的 EN/CN 内容一一对应。
2. **整段漏译（A）**：0 处。原文 935 叙事段 ↔ 译文 CN 935 段，段数、顺序、位置全等。
3. **章末截断（A）**：0 处。13 章末块均完整收到原文末句，例如 13 章末尾（原文 `…reached for the dictation mike.`）对应译文第 335 行「他走了。维伊巴不得过去就此了结、未来就此开场，伸手够向口述话筒。」
4. **重大数字错译（A/B）**：0 处。对全部含数字/数词的段做 EN↔CN 交叉核（`nums.py`）：`a billion credits → 十亿信用币`（6 章多处）、`five — six years ago → 五——六年前`（9 章）、`one thousand credits down, two thousand for training → 首期一千信用币，培训两千`（13 章）、`a third of the Kogan-Bors-Wazalitz control → 科根-博斯-瓦扎利茨三分之一的股控权`（1 章）、`seven bubble tents → 七座泡泡帐篷`（4 章）、`half a dozen silver-fins → 六条银鳍鱼`（4 章）、`four years—planet time → 四年——按行星年计算`（1 章）等**全部正确**。
5. **双侧同缺（专项③，本库 A 级主形态）**：**结构上不可能发生** —— 译文 EN 块与 `原文/*.md` **逐字节相同**（仅 FEFF），而 `原文/*.md` 与 epub `<p>` **元素级全等（935=935）**。任何「EN/CN 同时缺一段」都会立即在元素级对回中暴露。
6. **段数对称下的整段置换（专项③新判例）**：0 处。已做逐段语义对齐 + 全文重复段落检测（`dup.py`：同一中文段落全书出现 >1 次者 **0 条**、同一英文段落 >1 次者 **0 条**），无置换、无夹带。
7. **极性翻转**：0 处。双向否定极性核（EN 无否定而 CN ≥3 否定 / EN ≥2 否定而 CN 无否定）**无真实命中**，20 条初筛全部为工具正则未识别弯撇号 `’` 所致的假阳性。

## 四、B 级问题（应修）—— 共 3 类

### B-1 术语表硬约束违规 1 处（`Veep` → 帮主，应为「帮首」）

- 定位：`译文/08-chapter-8.zh-CN.md` 第 **99** 行（第 VIII 章第 6 块）
- 英文块原文摘录：`“This becomes more interesting,” the Veep had remarked with that deceptive gentleness.`
- 中文块译文摘录：「这就有意思了。」**帮主**当时的话带着那种骗人的温文。
- 定性：`术语表.md` 第五节明定 `Veep → 帮首`。全书 `Veep` 源文 **16 次**，译文 `帮首` 15 次 + `帮主` 1 次 → **1 处定名违规**。
- 可复现：`grep -n "帮主" 译文/*.md` → 唯一命中 `08-chapter-8.zh-CN.md:99`

### B-2 术语定名缩略 6 处（`civ / civs` → 「客户」，定名为「平民客户」）

- 定位与摘录（EN 括注为对应源句）：
  - `译文/04-chapter-4.zh-CN.md:169`：源 `when the civs were present` / `these stumble-footed civs` → 「当着平民客户他可从不这样」✓ 但「这些笨手笨脚的**客户**追上他」✗
  - `译文/04-chapter-4.zh-CN.md:273`：源 `The civ was trained` → 「这位**客户**性子虽急，到底是受过训的」✗
  - `译文/04-chapter-4.zh-CN.md:91`：源 `what the small civ had actually seen` → 「这个小个子**客户**到底看见了什么」✗
  - `译文/08-chapter-8.zh-CN.md:57`：源 `the civs lifted` → 「**客户**必须撤走」✗
  - `译文/08-chapter-8.zh-CN.md:83`：源 `one of your civs` → 「你的哪位**客户**能自己启动飞船发报」✗
  - `译文/08-chapter-8.zh-CN.md:159`：源 `If it weren't for those civs` → 「要不是惦着那几位**客户**」✗
- 统计：源 `civ/civs` 共 23 次，译文 `平民客户` **16 次**、`客户` 6 次、`平民猎客` 1 次。同时源 `client/clients` 亦译「客户」，两词译文在缩略形态上混同。
- 定性：**低烈度 B**。`术语表.md` 的说明栏本身写作「公会客户（相对猎人而言）」，故「客户」并非语义错误，且缩略处多为句子节奏所迫；但作为**硬约束层定名未按指定译法**，按九项第 5 条记 B，合并为 1 类。
- 可复现：`grep -n "这位客户性子虽急\|这些笨手笨脚的客户\|客户必须撤走\|你的哪位客户\|那几位客户——\|这个小个子客户" 译文/*.md`

### B-3 epub 非章节附属页未抽（文献附录类，建议可豁免）

- 缺口：`epub/text/` 中 `titlepage.xhtml`（`<h1>Star Hunter</h1>` + `By Andre Norton.`）、`imprint.xhtml`（`<header>Imprint</header>` + 4 段）、`colophon.xhtml`（`<header>Colophon</header>` + 5 段）、`uncopyright.xhtml`（`<h2>Uncopyright</h2>` + `<blockquote>` 1 段 + 4 段），**共 4 文件 14 段未抽入 `原文/`**。
- 占比与性质：14 段占 epub 全库 949 段的 **1.5%**，**全部为 Standard Ebooks 志愿者项目生成的版权/许可/版本说明（CC0 声明、字体与封面出处、修订号）**，非 Norton 叙事文本，正文内无任何段落指代这些内容（无指代悬空）。按 **B-4**「文献附录类材料」→ **B 级（应修）**，不进「必须修」清单；因**不满足 B-5 四条任一判据**（未达 10%、非整节/整篇叙事、无章末缺失、非集内某篇结尾），**不判 D**。
- 建议：`项目说明.md` 已将交付范围定义为「全书 13 章」，此 4 页属版本页而非作品正文，**可按项目惯例豁免**；如要求齐全，`epub_spider` 抽取时应加收这 4 文件。
- 复算命令：`python -c "import zipfile;z=zipfile.ZipFile(r'翻译项目/andre-norton_star-hunter/原书/andre-norton_star-hunter.epub');print([n for n in z.namelist() if n.endswith('.xhtml')])"`

## 五、C 级问题（建议优化）—— 共 6 类 / 约 95 处

| # | 类别 | 计数 | 定位与举例 |
|---|---|---|---|
| C-1 | 英文对照块字形档差异（U+FEFF 归一） | **79 段 / 94 字符** | 源文 Standard Ebooks 用 `\ufeff` 作 `—` 前断行禁止符；译文 EN 块将其剥离（如 `原文/01-chapter-1.md` 第 7 行 `Hume thought\ufeff—old` → 译文第 32 行 `Hume thought—old`）。按 B-1 口径属写盘管线全库性副产品，**记 C、单列计数、不据此下调总等级**。词级档为 0 |
| C-2 | 引号未闭合 | **2 处** | `译文/12-chapter-12.zh-CN.md:11` 段末「…就得赶在他们进那座山谷之前拦下来。」缺收尾 `」`；`译文/13-chapter-13.zh-CN.md:277` 段末「…为什么我没把在朱马拉的发现在档案里录带备案。」缺收尾 `」`。全书其余段落 `「」` 逐段平衡。复现：逐段 `count('「')!=count('」')` |
| C-3 | 中文表述瑕疵（语序打结 / 生造词） | **2 处** | ① `译文/11-chapter-11.zh-CN.md:35`「他再没有**剩不下**背着这么个分量站起来的力气，只好爬」——双否定令语义打结（源 `he had not the strength left to get to his feet with such a burden`），疑为多写「不」字；② 同文件 `:249`「休姆**目不斜离**操纵台」——「目不斜离」非规范成语（应为「目不斜视/目不转睛」）。两处语义均可复原，故记 C 不记 B |
| C-4 | 器物同物异形（flitter 系列） | **10 处** | 同一架公会飘行车在译文中出现三种形态：`飘行车` 42×（术语表定名）、`飞行车` 7×（08 章 4 处、09 章 1 处、11 章 2 处）、`飞行器` 3×（12 章 2 处、13 章 1 处）。**其中 6 处系源文自身在 `flitter` 与 `flyer` 间交替**（如 09 章 1 段源 `the flitter had landed … the flyer rested` → 「飘行车降落的那处山坳…飞行车就歇在台上」），属忠实镜像源文用词；另 4 处（08:215/247、11:45 等）源文无器物名词而译文补出「飞行车」。语义无歧义，记 C。另 `02 章` 的 `air-car → 飞行车`、`flitter → 飘行车` 区分正确 |
| C-5 | 部位描写失准 | **1 处** | `译文/10-chapter-10.zh-CN.md:125`：「矛尖直取那兽凸出的肚腹，刀锋咬进去的刹那向侧一拧，顺势一豁，**豁开一道大开的心口**。」源为 `raking his weapon to open a gaping wound`（未指部位），且本段前句已限定为「肚腹」、后文（10:141）亦作「那道**肚腹**的伤」，此处「心口」与「肚腹」自相龃龉 |
| C-6 | 术语语境变体 | **1 处** | `译文/04-chapter-4.zh-CN.md:87`：源 `want to make tri-dees of the pit-dragons` → 「要给穴龙拍**三维影像**」，非术语表定名「三维屏」。同书另 3 处 `tri-dee` 均作「三维屏」（02:127、04:239、04:259）。此处是「拍片子」的动词性语境，译「三维屏」反而不通，属**善意变通**，记 C 仅供参考 |

## 六、逐项清单结论

| # | 检查项 | 结论 | 缺陷数 |
|---|---|---|---|
| 1 | 语义错配 | 无 | 0 |
| 2 | 整段漏译 | 无（原文 935 叙事段 ↔ CN 935 段全等） | 0 |
| 3 | 章末截断 | 无（13 章末尾均完整收束） | 0 |
| 4 | 重大数字错译 | 无（含 billion/千分比/世纪/年代全部核对） | 0 |
| 5 | 术语表违规 | 有：`Veep→帮主` 1 处；`civ→客户` 缩略 6 处 | 2 类 / 7 处 |
| 6 | 同名异译 | 有（低）：`flitter` 系器物三种形态，其中 6 处随源文 `flitter/flyer` 交替 | 1 类 / 10 处 |
| 7 | 英文单词残留 | 无违规（剔括注后仅 `X-Tee×7`、`L-B×1`，均术语表明定照搬） | 0 |
| 8 | 语义误译 | 无（含极性/主体/时态双向核验） | 0 |
| 9 | 风格与流畅度 | 良好；个别表述瑕疵 2 处（C-3） | 2 处 |
| 专项① | 英文对照块逐字损坏 | **词级 0 处；字形档 79 段（全部 U+FEFF 剥离）** | 0 A/B / 79 C |
| 专项② | 段数三方对齐 | 原文 935(+3 `---`) / EN 935 / CN 935(+3 `---`)，**零不对称**。预检 3 处 `+1` 系 `---` 口径 | 0 |
| 专项③ | 双侧同缺 / 整段置换 | **均无**。EN 块与原文逐字节同、原文与 epub `<p>` 元素级全等（935=935），结构上排除；重复段检测 0 条 | 0 |
| 专项④ | 夹带与幽灵内容 | **无**。无章末重复对话、无杜撰署名块、无幽灵残句；无段落重复 | 0 |
| 专项⑤ | 全库型 IME 错字扫描 | 共 2,294 个不同汉字、49,568 字，逐一扫描仅 1 字/词量级问题（C-3 的「剩不下」「目不斜离」），**未发现「个月人」同族形近字错** | 2 处（C） |
| 专项⑥ | 原文素材完整性 | **13 章叙事文本 100% 抽全**（`<p>` 935=935、`<h2>` 13=13、`<hr>` 3=3、章内 `<blockquote>/<header>/<img>` 均 0）；仅 4 个 Standard Ebooks 版权/版本页（14 段、占 1.5%）未抽 | 1 类（B-3，可豁免） |
| 专项⑦ | 逐章译率核对 | 13 章中位 **0.344**，区间 0.316–0.368，**无章跌破 0.20**，无整章未译 | 0 |

## 七、等级判定理由

**判定：B 良好（B 档上限）。**

**未判 A 的依据（从严）**：A 档判据要求「数据层五项」中 **术语零违规**。本稿虽 80 条硬约束中 78 条 100% 落位，但仍有 **2 类**未达标（`Veep→帮主` 1 处、`civ→客户` 缩略 6 处），超出 A 档「零星」条款所容的「**单一**术语漂移 ≤1 类」。故机械对应判据落 **B 良好**。

**未判 C/D 的依据**：C 档判据为「A 级若干，或 B 级成片」；D 档判据为 B-5 四条结构性缺陷。本稿 **A 级 0 处**、B 级仅 3 类且**无一处成片或系统性**（术语偏差合计 7 处 token，占术语总量约 1%），B-5 四条（缺失≥10% 段落 / 整节整篇缺失 / ≥2 处章末终场缺失 / 集内某篇结尾缺失）**无一条成立**，故远不到 C/D。

**两可情形并列（供派发方裁量）**：

- *读法一（本报告从严采用，B 良好）*：`civ→客户` 视为「术语表定名未按指定译法」，术语类偏差 2 类 > A 档「≤1 类」，落 B。
- *读法二（可判 A 优秀）*：`术语表.md` 该行说明栏原文即写作「公会客户（相对猎人而言）」，可认定「客户」属术语表自身承认的等价变体，则术语偏差仅 `Veep→帮主` **1 类、1 处**，正落在 A 档「单一术语漂移 ≤1 类」之内；数据层其余四项（块段三方零不对称、英文块词级逐字一致、英文残留 0、数字全对）**全部满足**，且全文无 A 级缺陷。若派发方按此口径，本稿应为 **A 优秀**。

**客观质量陈述（不受等级选择影响）**：本稿是本批次中数据层最干净的交付物之一 —— 268 个块对零缺漏；935 个英文对照段落与 `原文/` **逐字节相同**（唯一差异是 Standard Ebooks 的 U+FEFF 被剥离）；`原文/` 对 `原书/*.epub` 的 13 章 `<p>` 元素 **935 = 935** 全等，凡本库已知的四类抽取失效模式（多 `<article>` 只抽首篇、`<img alt>` 漏抽、`<blockquote>/<header>` 漏抽、名单/题注漏抽）**均不成立**；无章末截断、无双侧同缺、无整段置换、无夹带幽灵内容、无数字错译、无英文残留违规；13 章译率全部落在 0.32–0.37 正常带内。**仅需**修掉 §四的术语两处与 §五的 C-2 引号两处即可达到 A 档。

## 八、元数据备注（不改文件，仅提示人工）

1. **`项目说明.md` 与实物一致，无需更正**：章节标题确为罗马数字 I–XIII，译文标题格式确为「罗马数字 / 中文」（`## I / 一` … `## XIII / 十三`）；体量标注「约 186KB」与实测 190,556 字节相符（「约」字内）。未发现张冠李戴。
2. **阅读序陷阱：无**。`epub_spine.py` 的 spine 序与两位零填充文件名字典序完全一致，可直接按文件名序处理。
3. **预检 10 项「未落位/落位偏低/幽灵条目」全部证伪**（逐条回读原文与译文后）：
   - `safari 源×29 译×7` → 假阳性。29 次中 **7 次为独立名词**（`set up a safari / plan a safari / run a safari / for a safari / safari ship / safari leader / a safari choice`）全部译「狩猎远征」；其余 22 次为复合词 `safari camp(16) / safari headquarters(2) / safari world(s)(1)` 等，译「狩猎营地／狩猎大营／狩猎星球」等，属自然复合，**非违规**。
   - `L-B 源×31 译×1` → 假阳性。`术语表.md` 该行说明栏明定「正文首次注明后用『救生艇』或照搬 L-B」，译文 `救生艇` **33 次** + 首现 `L-B（救生艇）` 1 次，**完全合规**。
   - `tape（动词）源×16 译×1` → 假阳性。译文按语境作「录制／录一盘带子／录带／带子／带机」，与术语表「语境区分证词录音与记忆植入带」的说明一致。
   - `the Center 源×3 译×1` → 假阳性。3 次命中中 2 次为普通名词误捕（`in the center of the port town → 港城中心`、`just above the center of the lake → 湖心正上方`），专名 `the Center` 实为 **1 次**，译「中心站」**1 次 = 100%**。
   - `Master（公会元老）源×2 译×1` → 假阳性。原文全库 `Master` 大写形仅 **1 次**（13 章 `reported to one of the Masters` → 「向一位元老报备」），另 1 次命中为 `strive to master his impatience`（动词）与 `If the master wants to drink`（小写普通名词，译「主子」），**均非公会元老**。
   - `psycho-tech 源×0 ★幽灵` → 假阳性。原文 01 章第 64 行实有 `psycho-techs`（复数），预检正则未匹配 `-techs` 词尾，**非幽灵条目**。
   - `Fata Rite…残留「永恒天平」×1` → 假阳性。同段落中 `Eternal Scales` 译「永恒天平」正是术语表指定译法（报恩平衡「永恒天平」），**非遗留**。
   - `port-drift 源×6 译×3` → 部分口径。译文作 2 种形态：指人时「港口流浪儿」（02:163/205、12:227 共 3 次），指生存状态时「港口流浪」（12:241/243、13:275 共 3 次），属语法性区分，语义无损，记 C 级以下不再单列。
   - `blaster 源×3 译×2` → 假阳性。第 3 处作动词化「一枪爆能」（02:13），`爆能` 形 3 次齐备。
   - `tri-dee 源×4 译×3` → 见 §五 C-6（语境变通）。
4. **工具口径提示（非项目缺陷）**：`scripts/check_coverage.py` 对本书 `原文/*.md` 报「原文段落数: 1」——因该脚本按**空行**切段，而本库 `原文/` 为「一段一行、无空行」的紧凑排版。本报告的段数统计改用**逐行切段**，得 935 段并与 epub `<p>` 元素全等。建议后续该脚本增加「无空行时按行切段」的回退逻辑。
5. **源文自身讹字（未构成缺陷）**：3 章英文块 `'Friend.' This was all Rynch could make out of that singsong of syllables Rynch knew to be a contact pattern.` 中作者重复了主语 `Rynch`；译文合并处理为「这是林奇从那串吟诵般的音节里唯一分辨得出的词」，属善意修正，按误报防线第 6 条记 C 级以下。另源文 `X-tee`（6 章）与 `X-Tee` 大小写不一，译文统一为术语表定名 `X-Tee`，亦属善意归一。
6. **`术语表.md` 与源文的一致性**：表内「tharsala lilly → 萨沙拉百合」保留了原文拼写 `lilly`（标准拼写为 `lily`），与 epub 一致，**非误植**；「Eucorian 源×1 译×1」等低频条目全部核实无误。

## 九、复核方法与局限

**精读范围（覆盖率 100%）**：对 `译文/01-chapter-1.zh-CN.md` … `13-chapter-13.zh-CN.md` **全部 13 个文件、268 个块对、935 个段对逐段精读**（英文块 ↔ 中文块逐段对读），未做抽样；同时以 `epub_spine.py` 的 spine 序为唯一阅读序，未按文件名假设。

**程序化核验（全部只读）**：块/段三方对齐与逐字节 diff（`analyze2.py`/`diff3.py`/`raw.py`/`feff.py`）；epub 元素级对回（`epubcmp.py`，逐 `<p>/<h2>/<hr>/<blockquote>/<header>/<img>` 计数与文本比较）；术语落位复算（`termdrift.py`/`gloss.py`/`civ.py`/`safari.py`）；英文残留（`residue2.py`）；数字交叉核（`nums.py`）；否定极性双向核（`polarity.py`）；长度比离群扫描（`outlier.py`，CN 汉字/EN 词中位 1.46，最低 0.67/最高 3.20 全部为感叹句与首现括注所致，无压缩型漏译）；稀有汉字扫描（`ime.py`，2,294 个不同字逐一过目）；重复段检测（`dup.py`）；mtime 与分片产物检查（无 `*.all.*`、无 `_partN`、13 章 mtime 单调递增 04:53→05:35，**无「未完成产物」特征**）。

**存疑 / 未覆盖**：

1. **`civ→客户` 的定级存在口径分歧**（见 §七两可并列），本报告从严记 B，请派发方按批次统一口径裁量。
2. `译文/11-chapter-11.zh-CN.md:35`「剩不下」是否确为多字讹误，只能从语义复原推断，**无法从写盘管线证实**，故按 C 记而非 B。
3. 未核 epub CSS/`onix.xml` 等非文本资源；未核 `原书/` 封面与插图（本书 epub 无正文插图，`images/` 仅 cover/logo/titlepage）。
4. 文学性评价（语调、节奏、意象再现）属主观判断范畴，本报告仅在发现可定位的具体瑕疵时列级；整体文风与 1960 年代太空歌剧语域契合，未发现机翻腔成片现象。
5. 未读取、未参考本项目旧 `审核报告.md`、`Plan.md`、`translation_queue.csv` 及任何批次记录与他项目复核报告（盲审红线遵守）；本报告全部结论仅源自 `原文/`、`译文/`、`术语表.md`、`项目说明.md`、`原书/andre-norton_star-hunter.epub`。
